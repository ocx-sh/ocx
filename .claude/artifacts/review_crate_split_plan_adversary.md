# Review: `plan_crate_split_workspace.md` — cross-model adversary (Codex)

**Reviewer:** `codex:rescue` (adversary, plan-artifact scope, one-shot) · **Triage:** hex-plan orchestrator (opus) · **Base:** `evelynn` @ `d8750fd7` · **Date:** 2026-09-16
**Input:** the plan as of round 1, ADR, design addendum, the four discover artifacts, the three research artifacts.
**Rule:** one-shot, no cross-family looping. Every finding was verified against the code before triage (`feedback_refuters_confirm_false_findings`); none was accepted on the adversary's word alone.

## Triage

| # | Sev | Verdict | Applied as |
|---|---|---|---|
| 1 | blocker | **Actionable** — a moved file's `use crate::…` cannot compile before rewrite | DEC-13 amended: one atomic commit per crate; C-049(ii) |
| 2 | blocker | **Actionable** — three `impl OciTransport for RecordingTransport` outside `ocx_oci` | DEC-14: `ocx_oci::testing::RecordingTransport` under `__testing`; C-053; WP-24/WP-31 files |
| 3 | major | **Actionable** — `AttestPipeline::run_inner` rejects before resolving | DEC-17: caller-supplied `resolve` closure; C-045; WP-18 adds `tasks/{sign,attest}.rs` |
| 4 | major | **Actionable (wording)** — `OCX_LOG` is a user surface; the rename is a real, if narrow, break | DEC-3 states the exception explicitly; Objective names it; `!` subject on WP-11 |
| 5 | major | **Actionable** — WP-14b/WP-17 both claimed `package_manager/**` | symlink move folded into serial WP-11 with the 26 callers enumerated; WP-17 file set narrowed |
| 6 | major | **Actionable** — WP-18/WP-19 both claimed `publish.rs` in wave 9 | WP-19 depends on WP-18 (wave 10); ownership chain recorded |
| 7 | major | **Actionable** — WP-38 shared `test_ai_config.py` with WP-08 without a dependency | WP-08 folded into WP-03; WP-38 depends on WP-04/05/06 + gate |
| 8 | major | **Actionable** — the scoped gate escalated on every phase-0/1 WP | DEC-15 honest count; C-019 rule (1) base from the full mark, rule (2) routing |
| 9 | major | **Actionable** — finalize recomposes commits with only scoped marks | DEC-6: `task release:prepare` full-mark guard in `cmds:`; C-021 |
| 10 | major | **Actionable** — addendum § A.1 still said `tracing::*!` after DEC-1 | Addendum A.1 reworded (`log::` macros); CapturingLogger kept; `log/std` dep |
| 11 | major | **Actionable** — no `-vv` exists | C-048 uses `--log-level debug`; discriminating red = `OCX_LOG=ocx_cli=debug` |
| 12 | major | **Actionable** — the pre-created shell makes "glob red before the move" vacuous | C-049(iii): move first, observe old glob red, then re-point |
| 13 | major | **Actionable** — manifest-text scan misses `package = "…"` aliases and target deps | C-009 reads the `cargo metadata` resolve graph; three synthetic reds |
| 14 | major | **Actionable** — a dead glob beside a live one passed | C-022: every glob must select ≥ 1 test |
| 15 | major | **Actionable** — collect-only floors are blind to skips | C-069/C-070 skip/xfail/ignored ceilings from run summaries |
| 16 | major | **Actionable** — `{:#}` on a concrete error cannot see a dropped `source()` | C-068: `render_chain` over a two-level cause |
| 17 | major | **Actionable** — no release feature-graph check | C-078 `release_feature_set_excludes_testing_seams` |
| 18 | major | **Actionable** — `shell-activation.yml` watches four moved paths | added to WP-11/27/28/34/36 file sets; red-first per C-049(viii) |
| 19 | major | **Actionable** — `tag_verdicts.json` moved with the index fixtures | WP-28 excludes it; WP-30 moves it with `tag_verdicts.rs` |
| 20 | major | **Actionable (honesty)** — `ocx_util`/`ocx_console` carry `tar`/`zip`/`indicatif` by the map | C-060 rewritten: forbids the Starlark family, `clap_complete`, `git2`, forge deps; states the console/util closure |
| 21 | major | **Actionable** — the baseline could not match the discover counts under D-037 | C-008: baseline measured under the corrected map; expected deltas named |
| 22 | minor | **Actionable** — 51 vs 57 files | WP-10: 57 files from the census |

**Verdict:** 22/22 actionable, 0 deferred, 0 rejected. Two blockers (1, 2) each reopened a decision the plan had taken from the ADR/research without checking the code path — both are now decided by measurement (DEC-13, DEC-14). None reopens an ADR ruling.

## Raw output (verbatim)

```
1. blocker · [7] `.claude/artifacts/plan_crate_split_workspace.md` C-049(ii), extraction implementation steps · “pure `git mv` + `mod` fixes, compiles” postpones dependency and consumer rewrites until commit 2 while explicitly rejecting temporary `#[path]` wiring. Moving `cli/error_category.rs` into `ocx_exit` leaves its `use crate::cli::ExitCode` referring to a module absent from that crate; retaining compilation through `ocx_lib` requires the rejected cross-directory wiring. · Make each extraction one atomic move-and-rewrite commit, or explicitly specify temporary path wiring and all adjustments needed to verify both intermediate trees.

2. blocker · [5] `.claude/artifacts/plan_crate_split_workspace.md` C-053 / WP-24 · “`OciTransport` sealed (private supertrait)” has no migration for existing downstream test implementations. `crates/ocx_lib/src/oci/attest/pipeline.rs:730`, `oci/sign/pipeline.rs:1300`, and `oci/verify/pipeline.rs:4385` each contain `impl OciTransport for RecordingTransport`; these remain outside `ocx_oci` when WP-24 seals it. A private supertrait deliberately prevents those implementations. · Add an explicit migration to configurable test transports implemented inside `ocx_oci`, preserving the existing assertions, before sealing the trait.

3. major · [4] `.claude/artifacts/plan_crate_split_workspace.md` C-045 / WP-18 · The adopted addendum § B, inversion 1.9, requires “a mandatory `resolved: &(Digest, Manifest)`”. In `crates/ocx_lib/src/oci/attest/pipeline.rs`, `AttestPipeline::run_inner` rejects unsigned unsupported predicate types, unsigned sidecar formats, and obsolete provenance before calling `resolve_platform_target`. `crates/ocx_cli/src/command/package_attest.rs` currently calls `attest_one(..., None)`. Resolving before entering the pipeline changes that ordering: an unreachable registry can replace the existing argument error with a network error. · Extract and invoke the existing preflight checks before resolution, preserving their error wrapping and order; explicitly include `package_manager/tasks/{sign,attest}.rs` in WP-18.

4. major · [4] `.claude/artifacts/plan_crate_split_workspace.md` DEC-3 / S-013 · DEC-3 permits target renaming while claiming a user directive is outside the protected output surface. `crates/ocx_lib/src/cli/log_settings.rs`, `LogSettings::build_env_filter`, reads `OCX_LOG` and `RUST_LOG` through `builder.from_env()`. `crates/ocx_lib/src/oci/client.rs`, `Client::pull_layer`, emits an untargeted `log::debug!`. Thus `OCX_LOG=ocx_lib::oci::client=debug` produces that stderr line before extraction and suppresses it afterward. · Preserve existing event targets, or record an explicit exception to unchanged output and amend the contradictory objective and scenarios.

5. major · [2] `.claude/artifacts/plan_crate_split_workspace.md` WP-14b / WP-17 Expected files, wave 8 · WP-14b claims “`package_manager/tasks/{patch_publish,pull,pull_local}.rs`”; WP-17 claims “`package_manager/**` symlink users”. The overlap is `crates/ocx_lib/src/package_manager/tasks/pull.rs`: lines 1051 and 1081 call `crate::symlink::create`. Both WPs occupy wave 8, with no dependency between them. · Make WP-17 depend on WP-14b and move it to a subsequent wave, updating the graph and batch instructions.

6. major · [2] `.claude/artifacts/plan_crate_split_workspace.md` WP-18 / WP-19 Expected files, wave 9 · Both rows explicitly claim “`package_manager/managed_config/publish.rs`”, meaning `crates/ocx_lib/src/package_manager/managed_config/publish.rs` after WP-16. Both occupy wave 9 and neither depends on the other. · Assign the shared edits to one WP or serialize WP-19 after WP-18.

7. major · [3] `.claude/artifacts/plan_crate_split_workspace.md` WP-38 Depends on / Parallelization · WP-08 owns “`.claude/tests/test_ai_config.py`”; WP-38 claims the same file but depends only on “WP-37”. Following the graph backward from WP-37 never reaches WP-08. Likewise, WP-38’s subsystem-rule sweep overlaps WP-05’s `subsystem-ci.md` and WP-06’s `subsystem-taskfiles.md` without ancestry. This violates the stated “declared ancestor” merge predicate. · Add a tooling-completion dependency joining WP-04 and WP-05 into the subsequent execution chain, and verify ancestry for every shared file.

8. major · [7] `.claude/artifacts/plan_crate_split_workspace.md` C-019 / DEC-15 · C-019 selects the entire “`merge-base origin/main HEAD...HEAD`” difference and escalates on “any path not under a workspace member”. WP-01 changes root `Cargo.toml`, `Cargo.lock`, and `taskfile.yml`; those remain in every later branch difference. Consequently every subsequent invocation escalates, contradicting the per-WP scoped cadence and WP-04’s five-minute measurement. · Introduce an explicit WP base or last verified checkpoint for local scoped checks; retain the branch-wide comparison for integration verification.

9. major · [7] `.claude/artifacts/plan_crate_split_workspace.md` DEC-6 / C-021 · “Every other commit accepts a fresh `scoped` mark” includes ordinary `refactor:` commits recomposed on `evelynn` during finalize. Neither a `release:` subject nor branch `main` identifies that operation. `.claude/artifacts/adr_crate_split_workspace.md` § Pre-commit hook instead requires: “write a scoped mark, run `/hex-finalize`, see it deny.” The plan replaces that enforceable condition with an assertion that finalize runs the gate. · Add an explicit finalize entry check requiring a full mark and test that exact scoped-mark rejection.

10. major · [7] `.claude/artifacts/plan_crate_split_workspace.md` DEC-1 / WP-09 · DEC-1 retains `log::` emissions, but the adopted `.claude/artifacts/design_spec_crate_split_inversions.md` § A.1 still requires replacing `CapturingLogger` with a subscriber under “`with_default`”. In `crates/ocx_lib/src/oci/index/local_index.rs`, `install_warning_capture` currently installs the process-global logger with `set_boxed_logger`. A scoped tracing subscriber alone does not install that logger or bridge `log` records. · Amend the adopted mechanism to retain `CapturingLogger` using the direct `log` dependency, or explicitly install and validate a bridge before replacing it.

11. major · [7] `.claude/artifacts/plan_crate_split_workspace.md` C-048 / S-013 · The logging proof requires a library debug line “under `-vv`”. `crates/ocx_cli/src/app/context_options.rs:135` instead declares `#[arg(short, long, value_enum)] pub log_level`; there is no global verbosity counter. Furthermore, `LogSettings::build_env_filter` at `crates/ocx_lib/src/cli/log_settings.rs:172` gives an explicit log level precedence over environment filtering, so adding `OCX_LOG=ocx_cli=debug` would not suppress that line when the level flag remains. · Specify valid invocations and separate the explicit-level test from an environment-only positive/negative filter test.

12. major · [1] `.claude/artifacts/plan_crate_split_workspace.md` C-049(iii), C-001, extraction Specify step · The prescribed red proof is “`task claude:tests` red on the re-pointed glob before the move”. C-001 already creates every destination’s `Cargo.toml`, `README.md`, and `src/lib.rs` in WP-01. A new `crates/ocx_oci/**` glob therefore matches before extraction; green cannot establish that the intended source moved or is covered. · Test coverage of named subsystem source files, and demonstrate failure using a genuinely absent path rather than the pre-created crate directory.

13. major · [1] `.claude/artifacts/plan_crate_split_workspace.md` C-009 · The direction checker examines “`[dependencies]` `ocx_*` entries”. A forbidden dependency can instead use an alias such as `project = { package = "ocx_project", path = "../ocx_project" }`, or appear under `[target.'cfg(unix)'.dependencies]`; neither is covered by that specified scan. The checker can remain green while Cargo resolves the forbidden edge. · Resolve actual package identities and inspect normal, build, and target-specific dependencies, with separate dev rules and negative fixtures for aliases and target tables.

14. major · [1] `.claude/artifacts/plan_crate_split_workspace.md` C-022 · “A selection of zero tests is a failure” protects only the aggregate selection. A crate mapping with one live glob and one stale glob still selects tests and passes while silently losing the stale glob’s entire coverage. · Require every positive mapping glob to resolve to test IDs, and add a fixture where one glob is unmatched while another still selects tests.

15. major · [1] `.claude/artifacts/plan_crate_split_workspace.md` C-069 / C-070 · The floors inspect “`pytest --collect-only`” and “`cargo nextest list`”, not execution. Skipping or ignoring tests preserves their collected identities and counts; adding new tests can also offset deletion of old ones. These contracts can stay green while the original checks never execute. · Preserve a baseline of test identities and expected skip status, and validate execution reports in addition to collection counts.

16. major · [1] `.claude/artifacts/plan_crate_split_workspace.md` C-068 / adopted D-049 · D-049’s `display_chain_is_transparent` test compares “`format!("{err:#}")`” with the inner error. Alternate `Display` on a concrete error does not independently walk `source()`. A wrapper forwarding `Display` but dropping its source passes that assertion. The actual output helper, `crates/ocx_lib/src/error.rs::render_chain`, explicitly calls `append_chain(..., error.source())`. · Test the rendered source chain and required downcasts using a multi-level cause, and prove that removing a source link makes the test fail.

17. major · [5] `.claude/artifacts/plan_crate_split_workspace.md` C-010 / C-066, Testing strategy · The plan checks `__testing` forwarding but assigns no release-feature exclusion check. `.claude/artifacts/adr_crate_split_workspace.md` § Non-functional requirements, Security (iv), explicitly requires release artifacts’ feature-off property to be “asserted by a feature-graph check on the release profile”. Forward-list equality does not prove release builds omit those features. · Add a contract, owning WP, and release-build feature-graph check that fails when any workspace `__testing` feature is enabled.

18. major · [5] `.claude/artifacts/plan_crate_split_workspace.md` WP-11 / WP-27 / WP-28 / WP-34, C-023 · `.github/workflows/shell-activation.yml` § `on.pull_request.paths` names `activation.rs`, `shim.rs`, `oci/index.rs`, and `package_manager.rs`, plus their subtrees. Those move in WP-11, WP-27, WP-28, and WP-34, but the plan assigns this workflow only to WP-32 and WP-36. C-023 therefore encounters stale filters before their scheduled repair, or the workflow stops covering those changes. · Add the workflow to every WP moving one of its watched paths and update only the corresponding filters in that move.

19. major · [5] `.claude/artifacts/plan_crate_split_workspace.md` WP-28 / WP-30 Expected files · WP-28 moves “`tests/fixtures/{index_wire,live_index_ocx_sh}/**`” wholesale, but WP-30 later moves `tag_verdicts.rs` and `index_wire/tag_verdicts.json`. Meanwhile, `crates/ocx_lib/tests/tag_verdicts.rs::verdict_cases` reads `"tests/fixtures/index_wire/tag_verdicts.json"` relative to its own manifest and panics when absent. `.claude/artifacts/discover_crate_split_file_map.md` § 6 explicitly excludes this fixture from the index move. · Exclude `tag_verdicts.json` from WP-28 and specify the split re-vendoring destinations and provenance handling when WP-30 moves it.

20. major · [5] `.claude/artifacts/plan_crate_split_workspace.md` C-060 / S-015 · The required normal dependency tree must contain none of “`indicatif`, `zip`, `tar`”, yet C-051 puts archive implementations in `ocx_util`, which C-060 directly requires. `crates/ocx_lib/src/archive/tar.rs::TarBackend` contains `tar::Builder`; `archive/zip.rs::ZipBackend` contains `zip::ZipWriter`. The planned `ocx_oci → ocx_console` path also reaches `ProgressManager`’s `indicatif::MultiProgress`. These gates contradict the retained crate contents. · Reconcile the forbidden-dependency contract with the accepted map before execution; either explicitly permit these transitive dependencies or design and schedule the separation needed to exclude them.

21. major · [6] `.claude/artifacts/plan_crate_split_workspace.md` WP-02 implementation gate / C-008 / C-046 · WP-02 requires inventory output “matching `discover_crate_split_edge_inventory.md` § 2 exactly”, while C-008/C-046 require D-037’s corrected allowances. The discover artifact counts `ocx_package_manager → ocx_trust` as 13 forbidden references and `→ ocx_shell` as 1 within its total 33; D-037 legalizes both. Those counts cannot simultaneously remain exact under the corrected policy. · Preserve the original inventory as historical evidence and require a separately measured corrected baseline with an explicit edge-by-edge reconciliation.

22. minor · [6] `.claude/artifacts/plan_crate_split_workspace.md` WP-10 Expected files · WP-10 names “the 51 impl-holding files” from discover § 7, but that section identifies 51 as the old ADR citation and records newly added implementations. A read-only source scan finds the stated 64 + 4 implementations in 57 distinct files, including `error.rs`; examples include `crates/ocx_lib/src/config.rs:719` and `crates/ocx_lib/src/claim/error.rs:146`. · Replace the stale count with the complete measured file list: 57 including `error.rs`, or 56 plus its separately listed entry.


```

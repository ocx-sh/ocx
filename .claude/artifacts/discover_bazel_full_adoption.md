# Claim diff: bazel-full-adoption.md dossier vs live tree

Worker: architecture-explorer (hex-architect high). Repo: /home/mherwig/dev/ocx-sion (branch sion).
Method: Read tool + `sed -n` line-anchored verification (proxy grep -n distrusted per instructions).

| Claim (dossier line) | Verdict | Evidence (file:line + verbatim quote) | Consequence for the ADR |
|---|---|---|---|
| **1a.** Dossier line 51-52: `bazel-quality.md` contains rule IDs BZL-JS-01 and BZL-JS-03. | **WRONG** | `bazel-quality.md` (the top-level index) never mentions BZL-JS-01 or BZL-JS-03. Its only BZL-JS references are `BZL-JS-02` (line 84, Non-Negotiable #2) and `BZL-JS-13, BZL-JS-16, BZL-JS-11, BZL-JS-12` (line 99, Non-Negotiable #17). BZL-JS-01 and BZL-JS-03 live in the depth file `.claude/rules/bazel-quality/typescript.md` at lines 93 and 94 respectively: `93: \| BZL-JS-01 \| Confirm a real \`pnpm-lock.yaml\` at \`lockfileVersion\` 9 or higher exists before wiring any \`npm_translate_lock\`; convert the repository to pnpm first (\`pnpm import\`) rather than pointing the rule at an npm or yarn lockfile "for now". \|` and `94: \| BZL-JS-03 \| Never point a lockfile attribute at a \`bun.lock\` and never write \`bun_lock =\` — the attribute does not exist; a bun-locked package converts fully to pnpm before rules_js can see any dependency. \|` | Dossier's exemption language ("BZL-JS-01/03 ... needs an explicit exemption", line 52-53) is directionally right about what the rules say but cites the wrong file for where they live — anyone opening `bazel-quality.md` looking for them will not find them. ADR must cite `bazel-quality/typescript.md:93-94`, not the index. |
| **1b.** `BZL-FLAG-01` content. | VERIFIED | `.claude/rules/bazel-quality/flags.md:79`: `\| BZL-FLAG-01 \| Pin \`.bazelversion\` to an exact three-component LTS semver. Never \`rolling\`, \`last_green\`, \`last_rc\`, \`latest\`, \`latest-N\`, a bare RC string, a floating \`X.x\`/\`X.*\`, or a commit hash — outside a deliberately disposable fixture directory that states the exemption in a comment beside it. \|` | Confirms dossier's "BZL-FLAG-01" reference (line 139, "keep `.bazelversion` for BZL-FLAG-01") is about *pinning format*, not about bazelisk-vs-bazel routing — that's a separate instruction in "The Gate" (see 1c). |
| **1c.** "The Gate" instruction: bazelisk vs `bazel` on `$PATH`. | VERIFIED | `.claude/rules/bazel-quality.md:50-51`: "Run it after every change, cheapest first, and through the pin — `bazelisk` reading `.bazelversion`, never a `bazel` on `\$PATH`." | Confirms dossier's `.bazelversion` retention rationale (line 138-139: "Plain `bazel` ignores `.bazelversion` (confirmed)"). The rule file agrees the pin authority must route through `bazelisk`, consistent with the plan's ADR needing to also install/require bazelisk (not just `ocx.sh/bazelbuild/bazel:9`) if this non-negotiable is to hold — dossier line 10 says Bazel 9 obtained "not via bazelisk", which conflicts with this Gate instruction. **Flag for the ADR**: dossier's own design (ocx-managed `bazel` binary, not bazelisk) directly contradicts this repo's own `bazel-quality.md` Gate rule, which the ADR must reconcile or explicitly override. |
| **2a.** Dossier calls bazel-adopt "the 11-step procedure" (line 32). | VERIFIED | `.claude/skills/bazel-adopt/SKILL.md:83-317`, "## The procedure" heading through "## The migration plan file". Exactly 11 numbered `###` steps: 1. Name the narrower fix and why it was rejected (L87) · 2. Measure the signals (L110) · 3. Write the go/no-go decision (L141) · 4. Choose the pilot (L158) · 5. Pin the Bazel version, and decide 8.x against 9.x (L176) · 6. Create MODULE.bazel and put the lockfile gate in on day one (L201) · 7. Layer the rc files (L227) · 8. Wire the cache, in this order (L253) · 9. Stand up the CI lanes (L283) · 10. Run the language branch for the pilot (L304) · 11. Write the migration plan file (L317). | Dossier's "11-step procedure as WP-0" framing is accurate; plan can cite step numbers directly. |
| **2b.** Step 3 (decision file), verbatim. | VERIFIED | `.claude/skills/bazel-adopt/SKILL.md:141-156`: "### 3. Write the go/no-go decision\n\nFill the decision file (schema in [references/go-no-go.md](references/go-no-go.md)) with one row per signal, each carrying its measured value and the command that produced it. Then write the verdict as a sentence naming which signal decided it. ... On a no-go, stop here. The file is the deliverable." | Matches dossier's framing of the decision file as the plan's "first deliverable" (line 32-33). |
| **2c.** Step 4 (pilot subtree), verbatim; smallest-subtree text; live-service-dependency text. | VERIFIED | `.claude/skills/bazel-adopt/SKILL.md:158-174`: "### 4. Choose the pilot\n\nThe pilot is the smallest subtree that produces something shippable, in one language, with no compile-time dependency on a live service. Not the subtree with the worst pain — that one is usually the one with the non-hermetic step, and fixing hermeticity first is the sequencing every case study that succeeded actually followed." Live-service check: `git grep -lE 'sqlx::query!\|DATABASE_URL\|localstack\|testcontainers' -- ':!*.md' ':!*.lock'`; "Empty output = no compile-time service dependency found in tracked source = any subtree is eligible, and you pick on size." | **Directly contradicts dossier's own framing at line 50-51**: "pilot = Rust unit tests although not the smallest subtree (no live-service dep, so eligible; it is the only stage hitting the pain)." Step 4's text explicitly rejects "worst pain" as the pilot-selection criterion ("Not the subtree with the worst pain") and requires *smallest* — the dossier picks Rust unit tests **because** it hits the pain, which is the exact anti-pattern step 4 names and item 5 of "What agents get wrong" (L384-386) restates: "Picks the painful subtree as the pilot... usually the one with the non-hermetic compile step." ADR must either find Rust unit tests are ALSO the smallest eligible subtree (separate justification needed) or flag this as a deliberate, justified deviation from bazel-adopt step 4 — not a step-4-compliant choice. |
| **2d.** `references/branches-python-ts-cpp.md` around BZL-JS-01/03/29. | VERIFIED (line numbers corrected) | Actual location is lines 85-148 ("## TypeScript" section), not 90-150 as loosely estimated. Verbatim, line 94-98: "**pnpm is not a preference, it is the ingestion surface.** The virtual store is the only `node_modules` layout that decomposes into discrete cacheable actions. There is no `bun_lock` attribute and nobody has filed for one, and `bun test` has no ruleset either — a bun-locked package converts to pnpm first or it does not adopt (BZL-JS-01, BZL-JS-03, BZL-JS-29)." And line 108-111: "**Adopting for a repository not already on pnpm is a "no" as a starting move.** The cost — conversion, `hoist: false`, phantom-dependency fixes — is paid before any Bazel benefit arrives. The answer flips only when the JS package is a minority slice of a polyglot migration already justified by other languages." | This is the single strongest finding against the dossier's website-chain plan: the skill's own guidance says a bun-locked repo either converts fully to pnpm or **does not adopt Bazel for JS at all** — there is no "coarse Starlark rule that never enters rules_js" carve-out documented anywhere in bazel-adopt or bazel-quality. Dossier line 82-85's "one coarse Starlark rule ... Bun and `bun.lock` stay; no pnpm lock, no rules_js" is an invented exemption with no textual support in either source; it must be justified in the ADR as a deliberate, named deviation (which the dossier does gesture at, "needs an explicit exemption", line 52-53) rather than presented as bazel-adopt-compliant. |
| **3a.** Dossier line 127-128: `adr_crate_split_workspace.md:19` records "Bazel parked by the dossier". | VERIFIED (line number and wording exact) | `.claude/artifacts/adr_crate_split_workspace.md:19`: "- [x] No new build system (Bazel parked by the dossier), no new dependency introduced by this ADR" | Dossier's citation is precise — no correction needed. This is the prior decision the new ADR reopens. |
| **3b.** Every Bazel mention in `adr_crate_split_workspace.md`. | VERIFIED — exactly 2 hits | (1) Line 19 (quoted above, under "Tech Strategy Alignment"). (2) Line 113: "...the documented false-negative record of predictive test selection (bazel-diff, turborepo #9234) that argues against impact-analysis tooling here." (full sentence spans lines 112-114). | Only two Bazel touchpoints in the whole 81 KB ADR: the one-line park decision, and an unrelated citation of bazel-diff as prior art against target-selection tooling (irrelevant to the current dossier's scope, which explicitly defers target selection too — consistent). |
| **3c.** ADR `**Status:**` / `**Superseded By:**` lines. | VERIFIED | Line 5: "**Status:** Accepted". Line 15: "**Superseded By:** —". (Also line 7: "**Accepted:** 2026-09-16".) | The new Bazel ADR, if it reopens this decision, should set this ADR's `**Superseded By:**` field (currently `—`) once ratified — a cross-reference the plan must include as a required edit, not currently listed in dossier's "Surfaces touched" section. |
| **3d.** § "Stability tiers and the ecosystem contract" heading line. | VERIFIED | Line 406: "### Stability tiers and the ecosystem contract" | Matches CLAUDE.md's citation of this ADR section (used for crate tier table) — no drift. |
| **4a.** sccache configured at `verify-basic.yml:92-100` (dossier line 20-21). | VERIFIED | `.github/workflows/verify-basic.yml:92-100`: `SCCACHE_ENABLED: ${{ secrets.SCCACHE_AWS_ACCESS_KEY_ID != '' }}` through `AWS_SECRET_ACCESS_KEY: ${{ secrets.SCCACHE_AWS_SECRET_ACCESS_KEY }}` (job `smoke`, env block). | Dossier's line citation is exact. |
| **4b.** Unit-test job names, nextest step, floor/skip-ceiling gate. | VERIFIED — but the two workflows differ in shape, which the dossier's G3.2 goal glosses over | `verify-basic.yml` job `smoke` ("Smoke (Linux)", L86-87): `Unit-test floor` step L210-211 (`run: task rust:test:floor`) → `Test` step L212-219 (`run: task rust:test:unit -- --profile ci`) → `Skip ceiling` step L220-221 (`run: task rust:test:ceiling`) → `Ceiling self-test` L227-228. `verify-deep.yml` job `build` ("Build & Unit Test (${{ matrix.job.name }})", L76-77) runs nextest **directly**, no Taskfile wrapper, no floor/ceiling steps at all: L135-136 `run: cargo nextest run --workspace --target=${{ matrix.job.target }} --profile ci --locked`. | **The dossier's G3.2 goal ("floor and skip-ceiling gates re-derived from BEP counts") implicitly treats both workflows as carrying the gate.** Only `verify-basic.yml`'s Linux-only `smoke` job does; `verify-deep.yml`'s 3-OS `build` matrix has no floor/ceiling check today. The plan must decide whether the Bazel lane also needs to introduce floor/ceiling gating into `verify-deep.yml` (a net-new control) or leave deep OS-matrix legs ungated as they are now — this is a design decision, not a mechanical swap. |
| **4c.** Which jobs exist, and their triggers. | VERIFIED | `verify-basic.yml` (L1-351): triggers `push:[main]`, `pull_request:[main, 'announce/**']`, `merge_group: {}` (L3-11); jobs `conventional-commits` (PR only, L28), `workflow-lint` (L44), `smoke` (L86), `smoke-acceptance` (`needs: smoke`, L268). `verify-deep.yml` (L1-458): triggers `workflow_dispatch`, `workflow_call` (with `inputs.full`), `push:[main]`, `pull_request:[main]` types `[opened, synchronize, reopened, ready_for_review]`, `merge_group: {}`, weekly `schedule` cron `'0 6 * * 1'` (L3-34); jobs `index-conformance-drift` (L54), `build` (3-OS matrix, L76), `cross-compile` (Windows ARM64 only, L184), `acceptance-tests` (`needs:[build, cross-compile]`, L220), `test-results` (`needs:[build, cross-compile, acceptance-tests]`, L378), `satellite-verify` (`continue-on-error: true`, non-blocking, L395). | Matches `subsystem-ci.md`'s "Verification tiers" table description exactly — no drift found between the rule doc and the live workflow files. |
| **4d.** Secrets referenced; any job gated on `github.ref == refs/heads/main`. | VERIFIED | Secrets referenced: `secrets.SCCACHE_AWS_ACCESS_KEY_ID`, `secrets.SCCACHE_AWS_SECRET_ACCESS_KEY` (both files, sccache env blocks), `secrets.OTEL_OTLP_AUTH` (both files, telemetry env), `github.token` (`verify-deep.yml:73`, `GH_TOKEN` for `gh api`). Ref-gated: `verify-basic.yml:123` `Rust Cache` step `with: save-if: ${{ github.ref == 'refs/heads/main' }}`; both files' `concurrency.cancel-in-progress` expressions exclude `refs/heads/main` (`verify-basic.yml:25`, `verify-deep.yml:51`). No job (as opposed to a step/cache-save condition) is itself gated to run only on `main` — the write-vs-read split the dossier's goal 2 (`PR lanes read anonymously, main writes`) wants for a Bazel cache credential does not exist as a precedent in either workflow today; it would be new plumbing, modeled on the sccache/OTEL secret-presence pattern (`secrets.X != ''`), not a copy of an existing "main-only job" pattern. | Confirms dossier's cache-credential design (line 196-197: "main push only via `--remote_header` from a secret... PRs read anonymously") has no existing analogue to copy verbatim — the sccache pattern (secret-presence toggles a step) is the closest precedent, cited correctly implicitly by dossier's phrasing. |
| **5a.** `rust:test:unit` task at `taskfiles/rust.taskfile.yml:226-256` (dossier line 148). | VERIFIED (near-exact) | `taskfiles/rust.taskfile.yml:226-255`: task `test:unit` — deps `install:nextest`; cmd `cargo nextest run --workspace --release --locked {{.CLI_ARGS}} 2>&1 \| tee {{.NEXTEST_RUN_LOG}}` under `set -o pipefail`, then `task: :telemetry:push`. (Task body ends at L255/256, `test:doc` starts L257 — off-by-zero-or-one, immaterial.) | Confirms the tee-into-a-log pattern the "bracket" (`test:floor` deletes it, `test:ceiling` reads it) depends on — this is the shape the Bazel BEP-based re-derivation (G3.2) must reproduce without a log file, since Bazel's own `bazel test //...` has no equivalent single-summary-line artifact; the plan needs a BEP-native floor/ceiling reader from day one, not a port of this grep. |
| **5b.** Floor/ceiling gate at `taskfiles/rust.taskfile.yml:591-649` (dossier line 148). | VERIFIED (exact) | `taskfiles/rust.taskfile.yml:591-649` — `test:floor` (L591-614) and `test:ceiling` (L616-649), `platforms: [linux]` on both. Floor check verbatim (L604-614): `- rm -f {{.NEXTEST_RUN_LOG}}` then `floor=$(cat {{.ROOT_DIR}}/crates/NEXTEST_FLOOR); count=$(cargo nextest list --workspace --release --locked --message-format json \| python3 -c '...'); [ "$count" -ge "$floor" ] \|\| { echo "nextest floor: $count tests listed, floor is $floor (crates/NEXTEST_FLOOR) — the unit-test set shrank" >&2; exit 1; }`. Ceiling reads the tee'd log's `Summary [...]` line against a hand-derived nextest 0.9.144 grammar regex (`SUMMARY_GRAMMAR`, L634-638) and compares the parsed `skipped` count to `crates/NEXTEST_SKIP_CEILING`. | Dossier's line citation is exact to the character. **Both gates are Linux-only (`platforms: [linux]`)** and both are `cmds:` (never `preconditions:`) specifically so `--force` cannot skip them — the Bazel-lane re-derivation (G3.2, "re-derived from BEP counts") inherits this Linux-only scoping unless the ADR states otherwise, and must also decide whether the re-derived check stays a shell parser of BEP JSON (same fragility class the comments here warn about for nextest's Summary grammar) or something more robust. |
| **5c.** `crates/NEXTEST_FLOOR` value. | VERIFIED | File content: `8213` | Matches dossier's Verification section (line 180): "BEP-derived total ≥ `crates/NEXTEST_FLOOR` on the same commit nextest counts 8213." Exact match — dossier's number is current, not stale. |
| **5d.** `rust:test:doc` task. | VERIFIED | `taskfiles/rust.taskfile.yml:257-278` — `desc: Run the workspace doctests`; `cmd: cargo test --doc --workspace --locked {{.CLI_ARGS}}`. Summary block explicitly states: "nextest cannot run doctests at all, so `test:unit` above never executes one and the unit floor/ceiling bracket cannot see them either." | Confirms dossier's own claim (line 81, 133): "Doctests stay on cargo (`rust:test:doc`)" is correct and consistent with this task's own documented rationale — no Bazel lane is expected to absorb doctests, matching the dossier's out-of-scope list (line 163). |
| **6.** `taskfiles/telemetry.taskfile.yml` — junit2otlp push shape (dossier line 22, 94-97). | VERIFIED | Full file is 81 lines, single task `push` (L36-81). Exact command (L68-74): `junit2otlp --service-name ocx-tests --trace-name "ocx-{{.SUITE}}" --repository-path "$norepo" --batch-size 512 --additional-attributes "ocx.suite={{.SUITE}},ocx.source=local,ocx.os=$os,ocx.git.sha=$sha,ocx.git.branch=$branch,host.name=$host" < "$pretty" > "$log" 2>&1`. Endpoint env var: `OTEL_EXPORTER_OTLP_ENDPOINT` (L22, sourced from `~/.config/ocx-telemetry/env`, mode 600). Auth env var: `OTEL_EXPORTER_OTLP_HEADERS=Authorization=Basic <base64 of ci:password>` (L23) — nginx basic auth per `subsystem-ci.md`. Protocol: gRPC only — header comment L25-28: "junit2otlp speaks OTLP over gRPC only, and the Go SDK reads the endpoint as a URL: `https` selects TLS, `http` selects plaintext, and a bare `host:port` fails outright with `dns resolver: missing address`." Silent no-op when unconfigured: `[ -n "${OTEL_EXPORTER_OTLP_ENDPOINT:-}" ] \|\| exit 0; command -v junit2otlp >/dev/null 2>&1 \|\| exit 0` (L50-51). | Confirms dossier's premise (line 21-22, 94-97, and G3.5) that the junit2otlp push is gRPC-only, basic-auth, silent-by-default — the exact shape a bespoke BEP→OTLP script (dossier's "Observability" bullet, line 93-97) would copy. **Caveat for the ADR**: `subsystem-ci.md`'s "Test telemetry" section documents two silent-failure classes already found in this exact pipeline (a 1 MB-per-line stdin scanner losing whole reports, a 2,048-span default export queue silently dropping spans) — the BEP→OTLP script must budget for the same class of defect from day one (unbounded queue size, oversized single lines) rather than rediscovering it, and the dossier's G3.5 acceptance ("Tempo span count == BEP target count for one build") is exactly the check `subsystem-ci.md` says was the *only* thing that caught the queue-drop bug here. |
| **7.** `taskfile.yml` — `verify`, `verify:scoped`, `default` tasks: what they call, in what order; where a Bazel lane hooks in. | VERIFIED | `default` (L73-81): `deps: [git:hooks, rust:format:check, rust:clippy:check, check]`. `verify` (L93-127): `cmds: [git:hooks, .verify:lint, .verify:build-test, .verify:mark(SCOPE=full)]`. `.verify:lint` (L276-286, internal, parallel `deps:`): `rust:format:check, rust:clippy:check, shell:verify, ci:verify, claude:verify, scripts:verify, test:lint, website:lint:links`. `.verify:build-test` (L288-331, internal, sequential `cmds:`): `rust:license:check, rust:license:deps, rust:license:notice:check, scripts:suite-census, scripts:dead-path-sweep, rust:lint:ratchet, rust:doc:ratchet, rust:build, rust:test:floor, rust:test:unit, rust:test:ceiling, rust:test:ceiling:self-test, rust:test:doc, test:parallel`. `verify:scoped` (L129-251) branches on `scripts/scoped_gate.py --plan`'s decision (`escalate` → full `verify`; `routed`/`scoped` → a subset). | The natural Bazel-lane hook point is inside `.verify:build-test`, replacing the `rust:test:floor → rust:test:unit → rust:test:ceiling → rust:test:ceiling:self-test` sub-sequence (4 of 13 steps) with a `bazel test //crates/...`-backed equivalent — everything else in the phase (license checks, doc ratchet, `test:parallel`) is untouched by the dossier's scope. `verify:scoped`'s per-crate `nextest -p <crate>` step (not shown above but referenced at L152-155) is a **second** call site that would also need a Bazel-native per-crate equivalent if the scoped gate is to keep its fast local-loop property — the dossier's "Surfaces touched" list (line 148, "`taskfile.yml` + `taskfiles/rust.taskfile.yml`") does not call this out explicitly; the plan should. |
| **8.** Website build DAG (dossier line 82-85, 136-137: "schema → doc-scripts → recordings → sbom → vitepress"). | VERIFIED, with one correction to `subsystem-website.md` itself | `website/taskfile.yml` task `build` (L45-63): `cmds: [task: schema:default, task: scripts:publish, task: recordings:parallel, task: sbom:generate:page, bunx vitepress build]`. `schema:default` (`website/schema.taskfile.yml:163`) aggregates all 7 `generate*` tasks (metadata/config/project/project-lock/patch/reports/execution-record, L30-72). `scripts:publish` (`website/scripts.taskfile.yml:10-19`) is literally "Publish doc scripts to `website/src/_scripts/` (PT1–PT7 contract)" — this **is** the dossier's "doc-scripts" stage. `recordings:parallel` (`website/recordings.taskfile.yml:52-69`) has `sources: [recordings/**/*.py, doc_scripts/**/*.sh, conftest.py, src/**/*.py, pyproject.toml, {{.OCX_BINARY}}]` — so `test/doc_scripts/*.sh` are cache-invalidation inputs to the recordings step, not a separately-built target. | Dossier's 5-stage DAG (schema → doc-scripts → recordings → sbom → vitepress) is **more accurate than `subsystem-website.md`'s own "Generated Content Pipeline" table and "Build chain" line**, which list only 3 generators (schema, recordings, sbom) and omit the `scripts:publish` (doc-scripts) stage entirely. This is a real drift between the subsystem rule doc and the live taskfile — worth a fix-forward note, separate from the Bazel ADR, since `subsystem-website.md` is stale on this point regardless of Bazel. For the Bazel plan: a coarse website rule's declared inputs must include `test/doc_scripts/**/*.sh` (via the doc-scripts/recordings dependency) in addition to `website/**`, contradicting dossier's own inputs list (line 83-84: "inputs = `website/**` + generated schemas/casts/SBOM page") which omits `doc_scripts/`. |
| **9a.** `test/doc_scripts/` script count. | VERIFIED — 72, not enumerated by the dossier as a count | `find test/doc_scripts -name '*.sh'` → 72 files, flat directory (no subdirectories). Full list captured (72 scripts spanning `command-line__pinned.sh`, `deps*.sh`, `entry-points__*.sh`, `getting-started__*.sh`, `in-depth__*.sh`, `lazy-loading__lifecycle.sh`, `package-*.sh`, `patches__*.sh`, `promote__*.sh`, `select-deselect.sh`, `shell-integration__*.sh`, `user-guide__*.sh` (30+ of these), `variants.sh`, `index.sh`). | The dossier's G3.3 goal ("Every `test/doc_scripts/*.sh` is a cast action") implies 72 individual Bazel actions/targets if done literally per-script — a meaningful granularity decision for the plan, not mentioned as a concrete number anywhere in the dossier. |
| **9b.** `cast_recorder.py` uses pexpect (dossier `:377`). | VERIFIED (exact line) | `import pexpect` at line 9; `self._shell = pexpect.spawn(` at **line 377** — dossier's citation is exact. | No correction needed. |
| **9c.** `cast_recorder.py` omits wall-clock `timestamp` header (dossier `:72-86`). | VERIFIED (exact lines) | `CastRecording.to_cast()`, lines 72-86: the header JSON is `{"version": 2, "width": self.width, "height": self.height, "title": self.title}` (L73-78) — no `timestamp` field anywhere in the header or per-event tuples (`[round(event.timestamp, 3), event.event_type, event.data]`, L81-85, which is the asciinema-format **relative** frame timestamp, not a wall-clock one). | Dossier's line range and claim are exact — confirms cast byte-level content has no wall-clock dependency baked into the header, supporting the dossier's "cast byte reproducibility is a non-goal" framing (line 89) by showing the *header* at least is already reproducible; per-event relative timestamps and terminal-driven nondeterminism (pexpect timing) are the actual non-reproducibility source, not this header. |
| **10.** `crates/ocx_cli/build.rs` — env vars read, paths touched (dossier line 169-171). | VERIFIED | Full file is 119 lines. Env vars read directly via `std::env`/`env::var`: `CI` (L40, `std::env::var_os("CI")`, gates `vergen-gix`'s `build_timestamp`), `__OCX_BUILD_VERSION` and `__OCX_BUILD_CHANNEL` (L83-84, via `pass_through_env`), and six `GITHUB_*` vars in a loop (L87-96): `GITHUB_SERVER_URL, GITHUB_REPOSITORY, GITHUB_RUN_ID, GITHUB_WORKFLOW, GITHUB_REF, GITHUB_SHA`. Each emits `cargo:rerun-if-env-changed=<name>` (L106) — that is how Cargo, not the script itself, is told to treat these as build-graph inputs. Filesystem: `.git/` is read indirectly through `vergen_gix::GixBuilder` (L54-66, `gix` crate under the hood), gated to degrade gracefully (`Err` → `cargo:warning`, L71-73) rather than fail the build — a tarball checkout with no `.git/` still compiles. | Confirms dossier's claim verbatim: `CI`, `GITHUB_*`, and `.git/` via vergen-gix are exactly the reads. **Consequence for the ADR's open question (dossier line 169-171)**: under `--incompatible_strict_action_env`, Bazel actions get a minimal, explicit action environment — none of `CI`/`GITHUB_*` reach the action unless the ADR adds them via `--action_env=CI` etc. (defeating hermeticity/cache-key stability across CI vs local) or the build.rs script is patched to treat their absence as the tarball-checkout case it already handles gracefully (L71-73 already has this fallback for `.git/`; the `CI`/`GITHUB_*` vars have no such fallback — they simply resolve to `None` via `option_env!()`, which is silently correct, just cache-key-neutral). Bazel's `.git/` visibility needs `--workspace_status_command`/`--stamp` as the dossier notes — but the six `GITHUB_*` vars specifically only ever populate `ci.run_url` in `ocx version --format json` (per the file's own doc comment, L19-20) and have no bearing on build *correctness*, so the ADR can likely treat them as acceptably absent under Bazel (matching production release behavior for a non-GHA build) rather than requiring `--action_env` plumbing — worth stating explicitly rather than leaving as an open question. |
| **11.** `ocx.toml`/`ocx.lock` tool list; `bazelbuild/bazel` present?; `asciinema/agg` present?; identifier spelling convention; `.bazelversion` or any Bazel file anywhere. | VERIFIED | `ocx.toml` `[tools]` (9 entries, full list): `actionlint = "ocx.sh/actionlint/actionlint:latest"`, `bun = "ocx.sh/oven-sh/bun:1"`, `cosign = "ocx.sh/sigstore/cosign:3.1.1"`, `git-cliff = "ocx.sh/git-cliff/git-cliff:2"`, `go-task = "ocx.sh/go-task/task:3"`, `lychee = "ocx.sh/lychee/lychee:0"`, `shellcheck = "ocx.sh/shellcheck/shellcheck:0.11"`, `shfmt = "ocx.sh/shfmt/shfmt:3"`, `uv = "ocx.sh/astral-sh/uv:0"`. `ocx.lock` mirrors these as 9 `[[tool]]` entries with `repository = "ocx.sh/<org>/<name>"` and per-platform digests. **`bazelbuild/bazel` is NOT present. `asciinema/agg` is NOT present.** Identifier convention confirmed: `ocx.sh/<org-or-project>/<name>[:version]`, e.g. `ocx.sh/oven-sh/bun:1`, `ocx.sh/sigstore/cosign:3.1.1`. **No `.bazelversion`, `MODULE.bazel`, `BUILD.bazel`, `.bazelrc`, or any `*.bzl` file exists anywhere in the repository tree** (`find . -iname '.bazelversion' -o -iname 'MODULE.bazel' -o -iname 'BUILD.bazel' -o -iname '.bazelrc'` and `find . -iname '*.bzl'` both empty). | Confirms dossier's line 15-16 ("Repo has no Bazel files") and line 67 ("Every gap this exposes... a mirror package first"): both `bazelbuild/bazel:9` and `asciinema/agg` are genuinely new additions this ADR/plan would introduce to `ocx.toml`/`ocx.lock`, not already-staged tools — the dossier's framing that these need mirror packages built first (line 65-69, "a missing tool (`agg`) gets a mirror package first") is accurate and is real, unstarted prerequisite work, not a formality. |
| **12.** Root `Cargo.toml` — workspace member count, `exclude` list, `[patch.crates-io]`; `rust-toolchain.toml` pin; `scripts/crate_map.toml` edge-check shape + enforcer. | VERIFIED | `Cargo.toml`: `members = ["crates/*"]` (glob); actual count via `for d in crates/*/; do test -f "$d/Cargo.toml"; done` = **20** real members (matches CLAUDE.md's "20 workspace members" claim exactly). `exclude = ["external/rust-oci-client", "external/docker_credential", "external/sigstore-rs"]`. `[patch.crates-io]`: `oci-client = {path = "external/rust-oci-client"}`, `docker_credential = {path = "external/docker_credential"}`, `sigstore = {path = "external/sigstore-rs"}` — 3 entries, matching CLAUDE.md's "3 deps patched to submodules under `external/`". `rust-toolchain.toml`: `channel = "1.95.0"`, `profile = "default"`, `components = ["rustfmt", "clippy"]`, plus `targets = ["x86_64-pc-windows-gnullvm", "aarch64-pc-windows-gnullvm"]`. `scripts/crate_map.toml` (112 lines): a `[allowed]` TOML table keyed by Cargo package name → array of allowed dependency package names (e.g. `ocx_store = ["ocx_config", "ocx_oci", "ocx_util", "ocx_exit"]`), file header states it is read by `crates/ocx_test_support/tests/workspace_structure.rs::deps_direction` (confirmed present at that file's line ~500) which checks every `ocx_*` edge in the `cargo metadata` resolve graph against this table and reds naming `from -> to` on a violation; also read by `scripts/edge_inventory.py` (per the file's own doc comment). | Two leftover empty, git-untracked directories `crates/ocx_lib/` and `crates/ocx_mirror/` exist in this worktree (no `Cargo.toml`, not workspace members, not reachable by the `crates/*` glob's package resolution) — local cruft, not relevant to the canonical tree, but worth noting since a naive `ls crates/` overcounts to 22. **For the ADR**: `scripts/crate_map.toml`'s shape — a single flat TOML `key = [allowed-values]` table, enforced by a dedicated Rust test reading `cargo metadata` — is exactly the shape the dossier wants the Bazel BUILD-file drift check to copy (dossier line 77-78, "same shape as `scripts/crate_map.toml`'s edge check"); this confirms that request is well-formed and gives the concrete precedent file/test pair to model a `gazelle_rust`-generated-`BUILD.bazel`-vs-`Cargo.toml` drift check on. |
| **13.** `.gitignore` — `.agents/` ignored?; `target/`, `.tmp/`, `external/*/target` entries. | VERIFIED, with one nuance | `.agents/` is ignored (bare `.agents/` line, under "# Agent scratch: worktrees, logs, tmp — never committed"). `target/` is ignored (bare `target/` line near top). `.tmp/` is ignored (last line). **No explicit `external/*/target` entry exists** — but git's gitignore semantics mean a slash-less pattern like `target/` matches a directory of that name at *any* depth, so `external/rust-oci-client/target`, `external/docker_credential/target` and `external/sigstore-rs/target` are already covered by the bare `target/` line without a dedicated entry. | For `.bazelignore` (dossier line 144, "`.agents/`, `target/`, `.tmp/`, `external/*/target`"): Bazel does **not** share gitignore's "match at any depth" semantics for a bare pattern the same way — `.bazelignore` patterns are workspace-relative directory prefixes (each line is a path from the repo root, not a glob), so the dossier's literal `external/*/target` line is very likely still necessary for `.bazelignore` even though it is redundant in `.gitignore` — the two files' pattern languages differ and this is a real gotcha the plan should call out (do not assume `.gitignore`-parity is `.bazelignore`-parity). |
| **14.** `.claude/rules.md` — rows needing a change once `bazel-quality.md`'s path scope fires. | VERIFIED — exactly 2 existing rows | Line 58 (By concern table): `\| Bazel builds (\`BUILD\`/\`MODULE.bazel\`/Starlark) \| [bazel-quality.md](./rules/bazel-quality.md) (shareable), skills \`bazel-adopt\`, \`bazel-diagnose\` \|`. Line 113 (By auto-load path table): `\| \`**/BUILD.bazel\`, \`**/*.bzl\`, \`**/MODULE.bazel\`, \`**/.bazelrc\` \| [bazel-quality.md](./rules/bazel-quality.md) \|`. | Both rows already exist and are already correct — the catalog needs no *new* row once real `.bzl`/`BUILD.bazel`/`MODULE.bazel` files land, since the `paths:` glob in `bazel-quality.md`'s own frontmatter (confirmed at claim 1, lines 2-18) already covers `MODULE.bazel.lock`, `REPO.bazel`, `WORKSPACE*`, `.bazelrc*`, `.bazelversion`, `.bazelignore`, `*.star`, `*.scl` — broader than what `rules.md`'s auto-load table (line 113) currently lists. The catalog table is a subset of the rule's actual `paths:` frontmatter and should be widened to match (a `meta-ai-config.md`-flagged drift, `test_catalog_covers_all_rules`/`test_path_overlaps_declared_or_absent` may or may not currently catch this — worth a `task claude:tests` run once files land). |
| **15.** `CLAUDE.md` § "Build & Development" and § "Project toolchain" quotes. | VERIFIED (exact lines) | Lines 59-71 = the full section. Line 61: "Task runner [\`task\`](https://taskfile.dev) (Taskfile v3). **Run \`task --list\` before invent ad-hoc commands.** Common: \`task\` (fast check), \`task verify:scoped\` (per work package; escalates to full when it must), \`task verify\` (full gate), \`task rust:verify\`, \`task test\`, \`task checkpoint\`. Cargo OK for finer control..." Line 63: "**Project toolchain.** \`ocx.toml\` lists \`actionlint\`, \`bun\`, \`cosign\`, \`git-cliff\`, \`go-task\`, \`lychee\`, \`shellcheck\`, \`shfmt\`, \`uv\`. \`ocx self setup\` wires a per-prompt hook that puts them on \`PATH\` when you \`cd\` in and takes them off when you leave... CI bootstraps the same set via the \`setup-ocx\` action. Taskfiles call the tools directly — no \`ocx package exec\` wrapping. For one-off overrides — e.g. testing a freshly built ocx, or invoking from a shell with no hook — prefix with \`ocx exec -- <cmd>\`." | No drift from the live document shown at session start — matches verbatim. This is the section the plan must update once `bazel` is added to `ocx.toml` (dossier's Surfaces-touched list correctly names "`CLAUDE.md` Build & Development", line 152). |
| **16.** `subsystem-ci.md` / `subsystem-taskfiles.md` — cache sections. | VERIFIED — different shapes in each | `subsystem-ci.md` has **no dedicated "## Cache" heading**; caching content is a bullet under "## Verification tiers" (L12): line 22, "**Compile cache (2026-09-20)**: every Rust job runs \`sccache\` in front of the shared Garage S3 store at \`sccache.ocx.sh\`..." `subsystem-taskfiles.md` **does** have a dedicated heading: "## Caching Contract" at line 93, covering the `sources:`/`status:`/`method:` Taskfile fields, the `x-shared:` anchor pattern, and `.task/` cache directory (subsection at line 142). | The dossier's "Surfaces touched" list (line 154) names both files generically as needing a "cache section" edit; in practice the edit shapes differ — `subsystem-ci.md` needs a new bullet analogous to the existing "Compile cache" one (documenting the Bazel remote-cache wiring in CI), while `subsystem-taskfiles.md`'s "Caching Contract" section is about Taskfile's own `sources:`/`status:` fingerprinting and would need a *new subsection* (Bazel's cache is a different mechanism entirely, orthogonal to Taskfile's own caching) rather than an edit to the existing prose. |
| **17.** Contributing / dev-setup docs page under `website/src/docs/`. | ABSENT — confirmed, none exists | `website/src/docs/` top-level pages: `changelog.md`, `docker.md`, `faq.md`, `getting-started.md`, `installation.md`, `roadmap.md`, `user-guide.md`. No `contributing.md`, no `contributing/` directory, no dev-setup-equivalent page. | The dossier's own phrasing already hedges this correctly: "`website/src/docs/contributing` (**or equivalent dev-setup page**)" (line 153) — there is no such page today, equivalent or otherwise. The plan's docs surface for the Bazel workflow has no existing home to land in; it needs a **new** page (or a new section in `getting-started.md`, the closest existing analogue — it documents `ocx self setup`/project toolchain activation), not an edit to an existing contributing guide. |

## Additional sweep (non-exhaustive claims beyond the 17 above)

| Claim (dossier line) | Verdict | Evidence | Consequence |
|---|---|---|---|
| Line 25-27: `../rules_ocx`, `../mirror-bazelbuild` sibling repos. | OUT-OF-REPO — not read, per instructions | Both paths resolve outside `/home/mherwig/dev/ocx-sion`. | Cannot verify rules_ocx's "pinned 8.7.0" claim or ocx-mirror's contents from this worktree; the ADR must treat these as owner-supplied facts, not independently confirmed here. |
| Line 108-113: hetzner1 server inventory (`bazel-remote-cache:v2.6.2`, `bazel-cache.ocx.sh`, nginx htpasswd auth, no gRPC port, Prometheus/Grafana dashboards). | UNVERIFIED — infrastructure state, not a repo path | No file in this repository encodes this; it is an `ssh hetzner` / live-infra claim outside the scope of a repo-tree check. | Flag for the ADR: this is the single largest unverified-from-repo input to the whole plan (the entire remote-cache-reuse decision rests on it) — worth an explicit "owner-attested, not independently verified" note in the ADR's evidence section. |
| Line 101-106: six `.agents/research/bazel_*.md` files "(landed)". | VERIFIED — all 6 exist | `ls .agents/research/` shows: `bazel_bes_otel_ci_cache.md`, `bazel_bun_vitepress.md`, `bazel_codebase_recon.md`, `bazel_priorart_scan.md`, `bazel_pty_recordings.md`, `bazel_rules_rust_patched_deps.md` — matching all 6 filenames the dossier cites. | Research inputs are real, not invented citations. |
| Line 120: "Post-split sample (split landed 2026-09-19, `b79abbe8`)". | VERIFIED (exact) | `git log -1 --format='%H %ci' b79abbe8` → `b79abbe8d39192f33c55a752cc3bca12549c0623 2026-09-19 23:02:52 +0200`, subject `refactor!: dissolve ocx_lib into 17 responsibility-derived crates`. | Commit hash and date are accurate, not fabricated. |
| Line 76: "20 crates + 3 patched submodules" for gazelle_rust-generated `BUILD.bazel` per crate. | VERIFIED | Matches claim 12's finding exactly: 20 real Cargo workspace members + 3 `external/` patched submodules (`rust-oci-client`, `docker_credential`, `sigstore-rs`). | Consistent, no correction needed. |

## Summary

17 numbered claims plus a 5-item additional sweep, all logged above. Tally of non-VERIFIED findings against the dossier's own text (excluding items that were correctly hedged as UNVERIFIED/OUT-OF-REPO by the dossier itself):

- **WRONG**: 1 (claim 1a — BZL-JS-01/BZL-JS-03 live in `bazel-quality/typescript.md`, not `bazel-quality.md`).
- **CHANGED / needs correction or contradiction flagged**: 3 — claim 1c (dossier's "not via bazelisk" plan contradicts the repo's own bazel-quality Gate rule), claim 2c (dossier's pilot-selection rationale contradicts bazel-adopt step 4's explicit anti-pattern #5), claim 2d (dossier's "coarse Starlark rule... no pnpm lock" carve-out has no textual support in bazel-adopt/bazel-quality and contradicts branches-python-ts-cpp.md's "no as a starting move" guidance).
- **ABSENT**: 1 — no contributing/dev-setup docs page exists (claim 17), and separately no `.bazelversion`/`MODULE.bazel`/`BUILD.bazel`/`.bazelrc`/`*.bzl` anywhere (claim 11) — both dossier already stated correctly as absent, not contradicted.
- All other claims (4a-4d, 5a-5d, 6, 7, 8, 9a-9c, 10, 12, 13, 14, 15, 16, and the additional sweep) were VERIFIED, several with exact line-number precision, with secondary consequences noted for the ADR/plan authors.

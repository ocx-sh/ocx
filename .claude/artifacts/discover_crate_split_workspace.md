# Discovery: crate-split workspace claim diff + residual ground

Read-only exploration for the crate-split-and-verify-tiers ADR. Repo:
`/home/mherwig/dev/ocx-evelynn`. All git commands via `/usr/sbin/git`. No files
edited, no build run. Anchor date for the claim diff: 2026-09-06 (the
dossier's `Updated:` line). Only same-day commit found (`347d521b`, "move to
lore/grimoire ai skills") adds new `bazel-quality`/`docs-quality` rule
bundles — it touches nothing the dossier discusses, so effectively **no
commits changed the dossier's subject matter since the anchor**.

## Claim diff

| Path / claim | Status | Evidence |
|---|---|---|
| `crates/ocx_lib` ~280K LOC | **holds** | `find crates/ocx_lib/src -name '*.rs' \| xargs wc -l` = 278,777 total |
| 34 top-level modules in `ocx_lib` | **holds exactly** | `crates/ocx_lib/src/lib.rs:4-5` (`mod config;`, `mod media_type;`, private) + `lib.rs:39-70` (32 `pub mod` lines) = 34. My naive `ls src \| sort -u` gave 37 because it double-counted `lib.rs` itself and non-top-level dirs (`shims/` is a submodule of `shim.rs`, not its own `pub mod`) — the dossier's number is the correct one, verify against `lib.rs` directly, not a directory listing. |
| `shell_does_not_import_project` in `activation.rs` | **holds** | `crates/ocx_lib/src/activation.rs:1159` — and it is the **only** genuine module-boundary test in the repo (see Reusable code) |
| `try_classify` / `ClassifyExitCode` in `cli/classify.rs` | **holds** | `crates/ocx_lib/src/cli/classify.rs:44` (trait), `:104` (fn); file is 1,339 lines, `try_classify` contains 57 `try_downcast!` invocations and 45 `use crate::` lines (178 `crate::` tokens total in the file) — matches the "155 of 166 out-refs sit in try_classify" shape |
| `ClassifyExitCode` — 56 impls in 21 modules | **holds, approximately** | Multi-line-aware scan (`impl(?:<..>)?\s+(ClassifyExitCode\|ClassifyErrorKind)\s+for`) finds 61 impls across 23 top-level modules (a plain single-line grep undercounts to 34-39 because several impls are written `impl crate::cli::ClassifyExitCode for X`). 61/23 vs claimed 56/21 is close; the gap is plausibly impls added by in-flight branches not yet on `origin/main` (see In-flight work) — not a stale-dossier problem. |
| `TrustConfig`, `resolve_tiered`, `CompiledPolicy` in `trust.rs` | **holds** | `crates/ocx_lib/src/trust.rs:69,951,1271` (also `IdentityRule` at `:1157`); file is 3,458 lines |
| `oci/index` ~22K LOC | **holds, approximately** | `find crates/ocx_lib/src/oci/index -name '*.rs' \| xargs wc -l` = 20,265 (13 files). Close enough to not be a discrepancy given rounding. |
| `utility/fs/assemble.rs` ~3.8K LOC | **holds exactly** | 3,819 lines |
| `composer.rs.all` / `.fns` git-tracked | **holds** | `git ls-files` lists both under `crates/ocx_lib/src/package_manager/` alongside `composer.rs` |
| `task` targets `verify`, `.verify:lint`, `.verify:build-test`, `verify:mark`, `test:parallel` | **holds** | `taskfile.yml:79` (`verify`), `:91` (`.verify:mark`), `:98` (`verify:mark`), `:107` (`.verify:lint`), `:117` (`.verify:build-test`); `test:parallel` invoked at `taskfile.yml:125` |
| `claude:tests` target | **holds** | Defined as `tests:` inside `.claude/taskfile.yml:35` (namespaced `claude:tests` when included from root); runs `uv run --directory tests pytest test_ai_config.py -v` |
| `.claude/hooks/.state/commit-verified`, 300s TTL | **holds** | File exists; `.claude/hooks/pre_commit_verification.py:11` — "Blocks the commit unless `task verify` was run recently (5-minute TTL)" |
| `.claude/tests/test_ai_config.py` asserts `ocx_lib` paths | **holds** | Line 58 references `"ocx_lib"`; line 334 documents a bug about `crates/ocx_lib/src/*.rs` matching 23 unrelated files |
| 5 research artifacts exist | **holds** | All five `.agents/research/research_crate_split_{recon,prior_art,archaeology}.md`, `research_test_impact_adjacent.md`, `research_crate_decomposition_patterns.md` present |
| `arch-principles.md` "Core vs Plugin Boundary" + known-drift note | **holds** | `.claude/rules/arch-principles.md:22` heading (dated 2026-07-16), `:28` "Known drift: `ocx-mirror` reaches into operational internals" |
| `adr_shell_env_addenda.md` A-45 | **holds** | `:1984` "A-45 — The per-prompt sequencing lives at `crate::activation`..."; `:2020` names the `shell_does_not_import_project` test hook |
| `adr_cli_repo_split_template.md` exists | **holds** | File present, Status: Accepted |
| oci 13 generic submodules (annotations, digest, endpoint, file_storage, manifest, manifest_builder, pinned_identifier, referrer, repository, resolve_target, simplesigning, ssrf, transport_policy) | **holds** | All 13 exist as `.rs` files or dirs under `crates/ocx_lib/src/oci/` |
| oci 8 mixed submodules (attest, client, copy, host_capabilities, identifier, platform, sign, verify) | **holds** | All 8 exist. Note: `layer_layout` also exists under `oci/` and is named in neither list — the dossier's own 13+8+1(index)=22 accounting has one uncategorized module; not a defect in what it claimed, just an omission worth the ADR closing. |
| Signing stack: 37 files, ~37.6K LOC (`oci/{sign,attest,verify}`, `oci/simplesigning.rs`, `trust.rs`, `codesign.rs`) | **holds exactly** | `find ... \| wc -l` = 37 files; `wc -l` total = 37,587 lines |
| `verify.rs` references `crate::trust` ~67 times | **holds, approximately** | Broader grep (`crate::trust\|trust::`) across `oci/verify/*.rs` finds 71; concentrated in `identity.rs` (19), `pipeline.rs` (21), `error.rs` (16) |
| `client` has 16 out-edges | **not independently verified** | Per-file `use crate::` counts don't reproduce a single "out-edge" number without the same graph-construction methodology the recon used (likely module-level dedup of targets, not raw `use` lines) |
| co-change: `oci`↔`package_manager` 49 of 341 commits | **not independently reproduced** | Total repo history is 893 commits; commits touching `crates/ocx_lib/src` at all = 367 (close to the 341 baseline, plausibly a different date cutoff or merge-commit handling); reproducing the specific 49-commit co-change figure needs the same script, not attempted here |
| `~45 module-local *Error` enums, 4 `From` impls | **refined, not "changed"** | Top-level `Error` in `error.rs` has exactly 33 variants and exactly 4 `From` impls — **holds exactly**. But direct grep for `^pub enum [A-Za-z]*Error(\s\|\{)` (excluding `*Kind` enums) across `ocx_lib/src` finds **63**, not ~45. The recon artifact itself flags this number as a rough estimate needing an audit (`research_crate_split_recon.md:223`) — this is not dossier drift, it's the recon's own caveat materializing: treat 63 as the better inventory count for the error-type migration, not ~45. |
| `utility/path.rs` and `utility/fs/path.rs` — two unrelated modules sharing a name | **holds** | Both files exist |
| `crates/ocx_cli` ~57K LOC | **holds, approximately** | 56,585 total lines |

No path named by the dossier was found missing, moved, or contradicted by content — the single-day commit since the anchor is unrelated, and every discrepancy found is a counting-methodology gap (already flagged as approximate in the dossier's own research) or a plausible in-flight-branch drift, not stale information.

## Sibling-repo paths not diffed

- `../ocx-mirror` and `../ocx-mirror/.claude/artifacts/adr_mirror_signing.md` — outside the repository root (`realpath` resolves to a sibling checkout, not inside `/home/mherwig/dev/ocx-evelynn`). Not read per task scope.
- `../grimoire` — same; referenced only in the dossier's Addendum as the second lockstep consumer. Not read.

## `ocx_cli`

- Top-level layout: `api/` (thin — `api.rs` + `api/data.rs` + `api/data/` with 41 files, ~17,001 LOC), `app/`, `command/` (75 files), `options/`, plus `build_receipt.rs`, `conventions.rs`, `error_envelope.rs`, `main.rs`, `lib.rs`.
- **The `api` layer is a report/schema facade, not a full indirection layer.** `api.rs:19` defines `trait Printable: serde::Serialize` and `api/data/*` holds ~51 concrete report structs (`About`, `AnnounceReport`, `StatusReport`, …). `ocx_lib::cli::data_interface::DataInterface` (the runtime dispatch struct) lives in the lib; `Printable` and the concrete report *types* live in the CLI crate. This is the split the dossier's classify/`cli`-hub inversion questions are already circling, extended one layer further.
- Commands mostly call `ocx_lib` directly rather than exclusively through `api`: 61 of 75 `command/*.rs` files `use ocx_lib::` directly; 33 use `crate::api` (overlap exists — files often use both). `adr_cli_high_low_layering.md` (Accepted, 2026-05-08) governs the `run`/`exec` project-vs-OCI-tier split, not an api/command layering rule; `adr_cli_plugin_pattern.md` (git-style external subcommands) is still faithfully implemented — `command.rs:170-171` (`#[command(external_subcommand)] External(Vec<OsString>)`), dispatched in `app.rs:177` via `app/plugin_dispatch.rs`.
- Top `ocx_lib::` import surface from `ocx_cli` (proxy for coupling a split must preserve): `cli` (216 refs), `oci` (211), `project` (103), `package_manager` (71), `package` (52), `env` (50), `error` (39), `shell`/`setup` (26 each), `trust`/`record` (21 each), `file_structure` (16), `lazy` (15), `managed_config` (14). `cli` and `oci` dominate exactly as the recon's hub finding predicts — from the consumer side, not just the internal graph side.
- Exit-code classification (`classify_error`/`ClassifyExitCode`) is invoked from 25 files across `app.rs`, `main.rs`, `error_envelope.rs`, `build_receipt.rs`, and most `command/*` files that do fallible I/O — this is a genuinely cross-cutting concern inside the CLI crate, not confined to `app.rs`/`main.rs`.
- Tests: 916 `#[test]` occurrences inside `crates/ocx_cli/src`; two integration tests in `crates/ocx_cli/tests/` (`linux_self_contained.rs`, `macos_self_contained.rs`).

## `ocx_schema` and `ocx_shim`

- **`ocx_schema` depends on `ocx` (the CLI crate) as a library, not only on `ocx_lib`.** Root `Cargo.toml:66-69`: `ocx = { path = "crates/ocx_cli" }` with the comment "The CLI as a library. Only `ocx_schema` links it, so that the published report contract is generated from the very types the CLI serializes." `crates/ocx_schema/src/reports.rs` pulls 51 types from `ocx::api::data::*` (the report structs just described) — this is a **third crate-boundary constraint the dossier's recon didn't surface**: any split has to keep `ocx_cli::api::data` reachable as a library target for schema generation, meaning the CLI crate (or whatever inherits `api::data`) cannot become schema-generation-inert.
- `ocx_schema` also imports `ocx_lib::{Config, package::metadata::authoring::AuthoringMetadata, patch::PatchDescriptor, project::{ProjectConfig, ProjectLock}, record::ExecutionRecord}` directly (`crates/ocx_schema/src/lib.rs:10-14`).
- `schemars::JsonSchema` derive usage inside `ocx_lib` by top-level module: `package` (19 files), `oci` (8), `config` (5), `project` (4), `shell`/`record` (2 each), `utility`/`trust.rs`/`publisher`/`patch`/`package_manager`/`lazy.rs`/`config.rs` (1 each) — 13 distinct modules. A crate split spreads schema-derived types across at least that many crates, and `ocx_schema`'s dependency edges (both `ocx_lib` and `ocx`) will need re-pointing to whichever new crates end up owning each type.
- `task schema` is a delegating alias (`taskfile.yml:26`) to `website/schema.taskfile.yml`, run from the `website/` directory — the schema task's home is outside `crates/` entirely.
- `ocx_shim` has **zero** dependency on `ocx_lib` or `ocx` — its `Cargo.toml` declares no `[dependencies]` at all except a Windows-only `windows-sys`/`dunce` pair. It is fully decoupled today; the dist-profile coupling is entirely in the root `Cargo.toml`'s dedicated `[profile.shim]` (`opt-level = "z"`, `lto = true`, `panic = "abort"`, comment explains a per-crate `[profile.release]` override isn't supported by Cargo, hence the separate named profile) and in `rust.taskfile.yml` (`cargo zigbuild -p ocx_shim --profile shim`, `cargo check -p ocx_shim --target {x86_64,aarch64}-pc-windows-msvc`). A crate split doesn't touch this crate at all.

## Workspace mechanics

- Root `Cargo.toml`: `resolver = "3"`; 4 members (`ocx_cli`, `ocx_lib`, `ocx_schema`, `ocx_shim`); `exclude = ["external/rust-oci-client", "external/docker_credential"]` (deliberate — lets those submodules run their own `cargo` commands unclaimed by the outer workspace); `[patch.crates-io]` repoints `oci-client` and `docker_credential` to the two `external/` submodules; `[workspace.dependencies]` has 91 entries (`grep -c "^[a-zA-Z_-]* = "`).
- Two named profiles beyond `release`/`dev`: `dist` (fat LTO, `opt-level = "s"`, stripped, for the distributed binary — deliberately not `panic = "abort"` because 13 `resume_unwind` sites need unwind) and `shim` (the Windows-shim size budget, separate because Cargo has no per-crate profile override).
- `ocx_lib/Cargo.toml`: one feature, `__testing = []`, documented as an internal escape hatch (axum/reqwest `__name` convention) gating the `__OCX_SELF_IMAGE` test seam; `exclude = ["src/test_data/**"]` — **but `src/test_data/` does not currently exist** on disk, so this exclude entry is presently a no-op (minor, not dossier-claimed, just noted). `ocx_cli/Cargo.toml` forwards the same feature: `__testing = ["ocx_lib/__testing"]` (line 24) — this fan-out pattern (CLI crate forwards each library crate's `__testing` feature) will need to repeat per new crate that carries a test seam.
- `cfg(any(test, feature = "__testing"))` usage (the actual gate form — a bare `cfg(feature = "__testing")` string search finds nothing) appears in exactly 3 modules: `announce/pipeline.rs` (clock pin seam), `forge/{gitlab,github}.rs`, `oci/identifier.rs` (the `__OCX_SELF_IMAGE` seam, with a loopback-registry assert guarding misuse even when the feature is on).
- `build.rs`: only `crates/ocx_cli/build.rs` exists; `ocx_lib`, `ocx_schema`, `ocx_shim` have none.
- `crates/ocx_lib/tests/`: 4 integration test files (`index_wire_conformance.rs`, `dispatch_conformance.rs`, `tag_verdicts.rs`, `live_index_wire.rs`) plus two fixture dirs (`fixtures/index_wire`, `fixtures/live_index_ocx_sh`).
- `external/`: two submodules, `rust-oci-client` (branch `ocx/integration`) and `docker_credential`, both consumed transitively via `[patch.crates-io]` by anything that pulls `oci-client`/`docker_credential` — i.e. currently `ocx_lib`.

## Cross-cutting seams

- **`log.rs` is not the tracing-init site — correction to the dossier's framing.** `crates/ocx_lib/src/log.rs` is 4 lines: `pub use tracing_log::log::*;` — a compatibility shim bridging the `log` crate's macros into `tracing_log`, nothing more. Actual subscriber/init logic lives inside `crate::cli` (`cli/log_level.rs`, `cli/progress.rs`, `cli/log_settings.rs`) — one more concern bundled into the `cli` hub, reinforcing why that module resists being cut cleanly.
- `env.rs` is 4,398 lines with `pub mod keys` nested inside (`env.rs:14`) — confirmed single-file, as claimed.
- `config.rs` (901 lines), `managed_config.rs` (64 lines — thin, most of the substance is in `managed_config/` submodules), `project.rs` (60 lines — likewise thin, substance in `project/`). Top consumers of `crate::config::` by top-level module: `oci` and `config` itself (7 each), `project` (5), `setup`/`managed_config` (3 each), `package_manager`/`utility`/`trust.rs`/`shell` (1-2 each).
- `lazy.rs` (815 lines) and `activation.rs` (1,372 lines) sit at the crate root as claimed — `activation.rs` is the A-45 boundary-test host.
- Global/static state: 29 occurrences of `OnceLock`/`Lazy<`/`static mut`/`thread_local!` across 9 files: `file_structure`, `launch.rs`, `oci` (3 files), `project`, `script`, `setup`, `shell.rs`. A crate split has to carry each of these across whatever new crate boundary its owning module lands in — none are currently shared across module boundaries via a common global-state crate.

## Prior decisions

| ADR | Status | Decision | Constrains the split |
|---|---|---|---|
| [`adr_shell_env_addenda.md`](../artifacts/adr_shell_env_addenda.md) A-45 | Accepted (owner decision) | `crate::activation` at the root, `shell/` imports no `project`, enforced by `shell_does_not_import_project` | **Supports** — literally the template the dossier proposes reusing for every other boundary |
| [`adr_cli_repo_split_template.md`](../artifacts/adr_cli_repo_split_template.md) | Accepted | Satellite repos vendor `ocx` as a submodule, take `ocx_lib` as a path dependency, not crates.io | **Supports**, and the dossier's "ecosystem tier" formalizes exactly this |
| [`adr_cli_plugin_pattern.md`](../artifacts/adr_cli_plugin_pattern.md) | Proposed (but implemented — see `ocx_cli`) | Git-style external subcommand dispatch via `#[command(external_subcommand)]` | **Supports** — dispatch mechanism is CLI-crate-local, orthogonal to the lib split |
| [`adr_cli_high_low_layering.md`](../artifacts/adr_cli_high_low_layering.md) | Proposed | `ocx run` (project-tier) vs `ocx exec` (OCI-tier); `all` group keyword | **Neutral** — a naming/UX contract on `ocx.toml` resolution, doesn't constrain crate boundaries directly, but its `compose_tool_set` machinery lives in `package_manager`, a hub |
| [`arch-principles.md`](../rules/arch-principles.md) "Core vs Plugin Boundary" (2026-07-16) | owner doctrine | Long-term split of `ocx_lib`; `ocx-mirror`'s internals reach recorded as "known drift", target "CLI for operations" | **Supports directly** — this discussion is that doctrine's execution; the dossier already retires the "known drift" note per its Decisions |
| [`adr_trust_policy.md`](../artifacts/adr_trust_policy.md) | **Proposed** (MAX-tier, one-way door — published TOML surface + JSON envelope) | Single validation site for trust policy (DRY, cost only paid when trust is checked) | **Conflicts / must be resolved first** — this ADR is still Proposed, not Accepted, yet the dossier's addendum already treats `trust.rs`'s shape (policy vs discovery vs verification) as settled enough to plan a signing-stack extraction around. The crate-split ADR should note it inherits an open upstream decision. |
| [`adr_windows_exe_shim.md`](../artifacts/adr_windows_exe_shim.md) | Accepted | Committed signed blob + arch-gated `include_bytes!`, no `ocx_shim` runtime dependency | **Supports** — confirms `ocx_shim` is intentionally dependency-free; nothing to reconcile |
| [`adr_shim_hermetic_zigbuild.md`](../artifacts/adr_shim_hermetic_zigbuild.md) | Accepted (revised) | `cargo-zigbuild` + windows-gnu, raised size budget | **Neutral** — build-mechanics only |
| [`adr_ai_config_path_scope_correction.md`](../artifacts/adr_ai_config_path_scope_correction.md) | Proposed | Source-work-surface scope (not planning-surface scope) for rule `paths:` globs | **Supports directly** — this is the exact mechanism the dossier's "AI config follows the crates" requirement depends on; should be read before re-scoping `subsystem-*.md` |
| [`adr_real_sigstore_stack_and_delegation.md`](../artifacts/adr_real_sigstore_stack_and_delegation.md) | (no explicit Status line found) | Sigstore stack shape | **Conflicts/unclear** — status ambiguous, worth the architect confirming Accepted before the addendum's signing-crate boundary work proceeds |
| [`adr_offline_verify_trust_cache.md`](../artifacts/adr_offline_verify_trust_cache.md) | (no explicit Status line found) | Offline verify trust cache | Same caveat — read before treating `verify`/`trust` as stable enough to cut |
| [`adr_codesign_inside_out_signing.md`](../artifacts/adr_codesign_inside_out_signing.md) | **Superseded** | Recursive inside-out signing with inode dedup | **Note only** — superseded ADRs sitting in the signing subtree the addendum wants extracted; the extraction should carry forward only the live decision, not this one |

Index-protocol ADRs (`adr_index_indirection.md`, `adr_index_root_variants.md`, `adr_index_routing_semantics.md`, `adr_public_index_registry_indirection.md`, `adr_oci_index_only_dispatch.md`, `adr_servable_index_snapshot.md`, `adr_variants.md`) are **all Accepted** — they collectively constrain `oci/index` as the dossier's "one unambiguously OCX-specific" OCI submodule; none of them contradict that framing, so this axis is low-risk for the split.

## In-flight work

`git worktree list` shows 3 active worktrees inside this repo plus its siblings; `gh pr list --repo ocx-sh/ocx --state open` returns 5 open PRs.

| Branch / worktree | PR | Top-level `ocx_lib` modules touched (vs `origin/main`) | Note |
|---|---|---|---|
| `feat/lazy-package-loading` (`.agents/worktrees/integration`) | — | `cli`, `codesign.rs`, `env.rs`, `file_structure`, `lazy.rs`, `lib.rs`, `oci`, `package`, `package_manager`, `project` | Touches the `cli`/`env`/`file_structure` hubs plus `lazy.rs` (the module A-45-style boundary work would want isolated) |
| `feat/index-claim-command` (`.agents/worktrees` / `ocx-sion`) | [#420](https://github.com/ocx-sh/ocx/pull/420) | `announce`, `claim` (**new top-level module, not in `origin/main`'s 34**), `cli`, `env.rs`, `forge`, `launch`, `lib.rs`, `oci`; also touches `ocx_schema/reports.rs` | **A genuinely new module** — `crates/ocx_lib/src/claim.rs` + `claim/{error,owners,request,root}.rs` don't exist on `main` yet. Any crate-map inventory frozen before this lands is stale the moment it merges; sequencing should either land this first or explicitly carry `claim` as a day-one addition to the map. |
| `feat/toolchain-activation` | [#426](https://github.com/ocx-sh/ocx/pull/426) | `activation.rs` (+ new `activate.rs`, `ladder.rs`), `cli`, `config`, `env.rs`, `error.rs`, `file_structure`, `launch.rs` | Touches `activation.rs` directly — the exact file hosting the A-45 boundary test; a crate split extracting `activation` needs to coordinate with whichever of this or the split lands first |
| `feat/shareable-store` | [#169](https://github.com/ocx-sh/ocx/pull/169) | `cli`, `codesign.rs`, `env.rs`, `error.rs`, `file_structure`, `hardlink.rs`, `lib.rs`, `package_manager`, `project`, `utility` (+ new `reflink.rs`) | Stale (last updated 2026-07-17) but still open; touches `file_structure`/`hardlink`/`reflink` together — relevant if `file_structure` is an early extraction target |
| `fork/integration-rebase` (`.agents/worktrees/fork`) | — | none under `crates/` | Single commit repointing the `oci-client` fork dependency; not a source-layering collision |

Every active branch touches `cli`, `env.rs`, and usually `file_structure` — exactly the modules the dossier's recon calls out as hubs. **Any crate-split work package that starts by extracting one of those three will conflict with essentially every open PR and worktree branch currently alive.** This is the strongest concrete argument for the dossier's own "layer in place before cutting a crate" sequencing decision, and for cutting leaf/generic modules (the 13 generic `oci` submodules, `package`) before touching hubs.

## Verification surfaces

- `.github/workflows/build-windows-shims.yml` already hand-maintains a `paths:` filter naming specific files inside `ocx_lib`: `crates/ocx_shim/**`, `crates/ocx_lib/src/shims/**`, `crates/ocx_lib/src/shim.rs` (lines 20-22, repeated at 31-33). It also runs `cargo nextest run -p ocx_shim -p ocx_lib --locked` (line 87) and `cargo build -p ocx_shim --locked` (line 277).
- `.github/workflows/shell-activation.yml` and `shell-activation-deep.yml` similarly hand-list `crates/ocx_lib/src/{setup.rs,setup/**,shim.rs,shell.rs,shell/**}` and `crates/ocx_cli/src/command/{self_group/**,shell_state.rs}` as their path trigger (lines 34-40) — a comment at line 24 states the rationale ("the feature reaches no third-party crate").
- **These two workflows are the closest existing precedent for the dossier's crate→command→test-glob table** — they already encode a hand-maintained module-path-to-CI-job mapping and will need literal path updates the moment any of the named files move to a new crate. They are exactly the kind of "quietly does less" risk the project's verification-honesty rule calls out: a stale path filter here fails silently (the job just never triggers) rather than erroring.
- `verify-basic.yml` / `verify-deep.yml` both build and cache `target/release/ocx_schema` as an artifact and note "a cold full compile of `ocx_lib` stalls the run" — confirming the single-binary-dominates-build-time framing the dossier uses to justify scoped verification.
- `rust.taskfile.yml` line 133: `cargo build --release -p ocx --features ocx/__testing --locked` is the one place that builds the acceptance-test binary; line 90-91 separately does `cargo check -p ocx_shim --locked --all-targets --target {x86_64,aarch64}-pc-windows-msvc`, confirming `ocx_shim` already has its own scoped CI lane independent of the rest of the workspace — a small existing precedent for per-crate scoped checks.

## Reusable code and fixtures

- **`shell_does_not_import_project`** (`crates/ocx_lib/src/activation.rs:1159`) is the *only* real module-boundary-enforcement test in the repository today. Every other hit for the word "boundary" (char-class boundaries, string/byte boundaries, cache boundaries, symlink-walk boundaries, tag-form boundaries) is incidental, not architectural. The A-45 pattern this test embodies (walk every `.rs` under a subtree, assert no `use crate::<forbidden>`) is the literal template to replicate per intended edge removal — there is no other boundary-test scaffolding to reuse or retrofit.
- **No cycle/acyclicity tool is wired up.** `cargo-modules` is not on `PATH`; no `guppy`-based script exists anywhere in the repo (`find . -iname '*module_graph*' -o -iname '*co_change*' -o -iname '*dep_graph*'` returns nothing). The "cycle gate in CI" open question in the dossier starts from zero infrastructure, not a half-built one.
- `deny.toml` and `.licenserc.toml` exist at the workspace root and already govern all 4 crates uniformly — a crate split multiplies members but these configs are workspace-scoped, so no change is forced there.
- `crates/ocx_lib/tests/{index_wire_conformance,dispatch_conformance,tag_verdicts,live_index_wire}.rs` plus their two fixture dirs are integration-level tests that already exercise the `oci::index` boundary from outside the crate — worth checking whether they survive an `oci_index` extraction unchanged (they compile against `ocx_lib` today, would need to move or gain a new target crate).
- `shell-activation.yml`/`build-windows-shims.yml`'s hand-maintained path filters (above) are reusable as a starting point for the crate→command→test-glob table, since they already enumerate exactly the kind of module-to-CI mapping that table needs.

## Top findings (condensed)

1. Every dossier claim checked out true; no code drifted since the anchor. The handful of numeric mismatches (impl count, module-local error-enum count) are pre-existing approximations the recon artifact itself flags, not staleness.
2. **`ocx_schema` depends on `ocx` (the CLI crate), not just `ocx_lib`** — 51 report types the schema generator needs live in `crates/ocx_cli/src/api/data/`. This is a crate-boundary constraint the dossier's recon never mentions and the ADR must account for.
3. `log.rs` is a 4-line compatibility shim, not the tracing-init site; real init logic is inside the `cli` hub (`cli/log_level.rs`, `cli/progress.rs`, `cli/log_settings.rs`) — one more reason `cli` resists a clean cut.
4. The "~45 module-local error enums" estimate undercounts by ~40%: direct enumeration finds 63 (excluding `*Kind` enums). Use 63 as the migration inventory size, not ~45.
5. An **open PR (`feat/index-claim-command`, #420) introduces a brand-new top-level module (`claim`)** not present in `origin/main`'s 34-module inventory — any crate map frozen now is stale the moment that PR lands; sequence around it explicitly.
6. Every currently active branch/worktree touches the `cli` and `env.rs` hubs, and most touch `file_structure` too — concrete evidence for the dossier's own "layer in place, extract leaves first" sequencing call, not just theory.
7. `adr_trust_policy.md` is still **Proposed**, not Accepted, and `adr_real_sigstore_stack_and_delegation.md` / `adr_offline_verify_trust_cache.md` have no clear Status at all — the addendum's signing-stack extraction plan rests partly on undecided upstream ADRs.
8. There is exactly one reusable architectural-boundary test (`shell_does_not_import_project`) and zero cycle-detection tooling wired up anywhere — the "boundary tests + cycle gate" plan starts from a clean slate, not an extension of existing infra.
9. Two CI workflows (`build-windows-shims.yml`, `shell-activation.yml`) already hand-maintain module-path-to-job filters for `ocx_lib` submodules — direct precedent (and a direct maintenance burden) for the planned crate→command→test-glob table; both will silently stop firing if the named files move without a matching workflow edit.
10. `ocx_cli` commands mostly bypass the thin `api` layer and call `ocx_lib` directly (61 of 75 command files) — the "high/low layering" ADR governs `run` vs `exec` semantics, not an api/command indirection discipline, so there's no existing enforced layer between commands and `ocx_lib` to lean on.

## Negative — what could not be determined here

- Exact graph-edge counts (~120 populated edges, 63 reciprocated pairs) and the precise "16 out-edges" for `client` were not independently reproduced — they require the same module-dependency-graph construction the recon script used (dedup of `use crate::X::*` targets to top-level module, presumably), not a raw `grep`/`wc` count.
- The specific "49 of 341 commits" `oci`↔`package_manager` co-change statistic was not reproduced; total repo history is 893 commits, and commits touching `crates/ocx_lib/src` at all number 367 (close to but not identical to the claimed 341 baseline, plausibly a different cutoff or merge-commit handling in the original script).
- Status of `adr_real_sigstore_stack_and_delegation.md` and `adr_offline_verify_trust_cache.md` could not be read from a `**Status:**` line — both may use a different metadata format; the architect should open them directly rather than trust this scan's absence-of-match as "no status."
- Whether `src/test_data/**` (excluded in `ocx_lib/Cargo.toml` but absent on disk) is dead config or a not-yet-recreated directory was not chased further — outside this task's scope but worth a one-line cleanup note.
- Sibling repos `../ocx-mirror` and `../grimoire` were deliberately not opened (out of scope per task instructions); their import surfaces are taken as given from the dossier's own citations to `research_crate_split_archaeology.md`.

# Review: `plan_crate_split_workspace.md` — architect seat

**Reviewer:** architect (hex-plan panel, tier xhigh) · **Base:** `evelynn` @ `d8750fd7` · **Date:** 2026-09-16
**Targets read in full:** `adr_crate_split_workspace.md` (Accepted), `design_spec_crate_split_inversions.md` (D-001…D-066, § A.4), the plan, `arch-principles.md`, `quality-core.md`.
Every code claim below was checked by opening the file; paths are `crates/ocx_lib/src/…` unless prefixed.

## Findings

| # | Severity | Location | Finding | Fix |
|---|---|---|---|---|
| 1 | **Block** | Plan § Work packages, wave 8 (WP-14b, WP-16, WP-17) | Four same-wave collisions, all from WP-17's `crate::symlink` re-spell fan-out: `utility/fs.rs` (WP-16 deletes `mod assemble;` line 4; WP-17 must add `pub mod symlink;` — not even in its declared set), `utility/fs/assemble.rs` (WP-16 moves it; WP-17 re-spells inside it — it is a symlink user), `package_manager/tasks/pull.rs` (WP-14b codesign call; WP-17 symlink user), `package_manager/activation.rs` (WP-16 `:327`; WP-17 symlink user). Also WP-17 *declares* `Depends on: WP-16` yet sits in WP-16's wave — a self-contradiction the sub-orchestrator will execute literally. WP-17's declared set omits 7 of the 26 symlink users (`hardlink.rs`, `reference_manager.rs`, `shim.rs`, `script/guard.rs`, `project/registry.rs`, `record/execution_record.rs`, `shell.rs` is listed) so the merge predicate rejects it anyway. | Smallest: fold the two remaining pure moves (`symlink.rs` → `utility/fs/symlink.rs`, `utility/fs/assemble.rs` → `file_structure/assemble.rs`, with their 26 + n re-spells) into serial WP-11 ("four relocations"). WP-17 then loses its WP-16 dependency and its fan-out, wave 8 is file-disjoint. Alternative: WP-17 → wave 9, WP-19 → wave 10, WP-20 → wave 11; either way enumerate the 26 files. |
| 2 | **Block** | Plan § Work packages, wave 7 (WP-13 vs WP-14a) | `ocx_cli/src/app/context.rs:177–180` is the `LogSettings::init_with_progress` site WP-13 must rewrite (WP-13 lists `app.rs`, which only holds `:185`), and WP-14a edits the same file at `:198` (`detect_and_cache(Some(path))`). Same wave, two writers. WP-13 also omits `app/plugin_dispatch.rs:27,50` (`LogSettings` import + `.init()`). | Declare `app/context.rs` + `app/plugin_dispatch.rs` on WP-13 and move WP-13 to wave 8 (nothing depends on it before WP-20; wave 8 has no `app/context.rs` writer). |
| 3 | High | Plan C-060 | Unsatisfiable contract: `cargo tree -p ocx_sign -e normal` **will** contain `indicatif` and `clap_builder` (`ocx_sign → ocx_oci → ocx_console`: `oci/client.rs:236` holds `cli::progress::ProgressManager`; `cli/styles.rs:6`, `cli/options/color_mode.rs:61` use `clap_builder`) and `zip`/`tar`/`flate2`/`zstd` (`ocx_util` owns `archive/*`, `compression.rs`; `ocx_lib/Cargo.toml:69–103`). Only `starlark*` is genuinely absent. WP-31's gate goes red by construction and the ADR's D2 scoring ("six-crate closure" vs Option D's "`indicatif`, `clap_builder`, `zip`, `tar`") overstates the saving. | Rewrite C-060 to assert absence of `starlark*`, `allocative`, `clap_complete`, `quick-junit`, `git2`/forge deps; state plainly that grimoire's closure carries `indicatif`, `clap_builder`, `zip`, `tar` via `ocx_console`/`ocx_util`. |
| 4 | High | Plan DEC-15, C-019, C-022, § Implementation steps (extraction Gate) | Cost model understated. Phase 1: C-022 seeds the table with `ocx_lib → every verb`, so every phase-1 WP's "scoped" gate runs the whole acceptance suite (WP-10…WP-19 = 10 full-cost runs, + WP-20). Phase 2: every extraction edits `.claude/rules/*.md`, `CLAUDE.md`, `test/tests/test_logging.py` — non-member paths — so C-019 rule (2) escalates **all 17** (WP-21…WP-37), not the 8 hub/ecosystem ones the Gate step admits. With WP-05/WP-09 (`.github`, root `Cargo.toml`) and WP-38: ≈ 31 full-cost runs vs the 4 `Verify: full` cells stated — ≈ 13 h of serialized gate at ~25 min. The scoped tier pays off only after WP-37. | Say it plainly in § Parallelization (the tiering is built *for* the post-split loop, not the migration). Cheap mitigation for phase 2: in C-019 rule (2), route `.claude/**` + `CLAUDE.md` to `task claude:tests` and a changed `test/tests/<f>.py` to that file — neither is impact analysis. |
| 5 | Medium | Plan DEC-1 (+ addendum A.1/D-001) | The rationale is wrong on mechanism and moot on facts: `tracing_log::LogTracer` dispatches `Event::new(meta, values)` with `Parent::Current`, so bridged `log` records **do** inherit an entered span exactly like native events; and no span is ever entered — the only span in the tree (`package_manager/tasks/pull.rs:150`) is used solely as an explicit `parent:` for one event. `.compact()` + `with_target(false)` (`cli/log_settings.rs:78–83,109–120`) renders both paths byte-identically. DEC-1 still stands on its second leg (487 vs 72 sites, one-token `use` edit). Real trap: `log/std` is on today only through `tracing-log`'s default `std` feature; once `tracing-log` leaves the library manifests, `log::set_boxed_logger` in `oci/index/local_index.rs:1533` needs `log = { features = ["std"] }` or `cargo nextest run -p ocx_lib` (and later `-p ocx_index`) fails to compile the test. Addendum A.1 still says the `CapturingLogger` test is rewritten to a `tracing_subscriber` layer — unnecessary under DEC-1 (sed `crate::log::` → `log::`, assertion untouched); the plan's WP-09 rightly omits `local_index.rs`, the addendum text should follow. | Correct DEC-1's basis to "smaller diff, no observable delta either way"; declare `log = { version = "0.4", features = ["std"] }` in the workspace block (C-029); strike the `CapturingLogger` rewrite from A.1/D-001. |
| 6 | Medium | Plan DEC-3, WP-21 | The `!` changelog line sits on a commit that changes no target: `cli/exit_code.rs` and `cli/error_category.rs` emit zero events. Targets first change at **WP-11** (`activation.rs` 8 sites `ocx_lib::activation` → `ocx_lib::package_manager::activation`; `index_store.rs` 2 sites) — by the plan's own criterion ("a user-supplied directive spelling `ocx_lib::…`") that is the first observable break; the crate-prefix break lands at WP-22 (`ocx_util`). Consistent with CLAUDE.md (changelog = commit subject, nowhere else) — only the placement is off. Same class: the ADR's named ecosystem break (`ocx_lib::Error::OciClient` → `ClientError`) lands with WP-24 under D-047; its subject should carry it. | Put one `!` line on WP-11's second commit worded for the series ("tracing targets follow the crate split: `ocx_lib::x` → `ocx_<crate>::x` by its end"); no `!` on WP-21; WP-24's subject names the `Error::OciClient` break. |
| 7 | Medium | Addendum § C rule 2; plan C-042, C-051 | E1 mis-homes `SerializationFailure` ("all constructed in `package_manager`"): `ocx_util` constructs it at `utility/fs/locked_file.rs:402` (`LockedJsonFile::write`) and, invisibly to a grep, via `#[from]` at `utility/serde_ext.rs:28,36` (`SerdeExt::read_json/write_json` — a prelude trait the ADR ships in `ocx_util`). Sending the variant to `ocx_package_manager` makes `ocx_util` reference upward at WP-22, and C-042's "utility files construct only `FileError`/`SymlinkWalkError`" cannot be met at WP-17. | Home it in `ocx_util::error` (lowest constructor) with the literal `"JSON serialization error"` + `#[source] serde_json::Error`; `SerdeExt` and `LockedJsonFile` return a two-arm transparent util enum over `FileError` + it; `ocx_package_manager` wraps transparently (its 3 sites keep the same text). Add the literal test to D-049's list. |
| 8 | Medium | Plan C-066, WP-37; ADR § NFR (iv) | `__testing` seam crates are **8**, not 7: the 17 seam files map to `ocx_oci, ocx_config, ocx_store, ocx_announce, ocx_index, ocx_package_manager, ocx_package, ocx_shell`. The missed one, `ocx_index` (`oci/index.rs:50`, `oci/index/ocx_index.rs:299–329`), carries a `#[cfg(not(…))]` sibling — the silent-substitution shape (shipped 300 s index timeouts in the acceptance run). C-010's grep-derived test will demand it, so the number in C-066 is what's wrong. | Change "seven" to eight in C-066 and name `ocx_index`; forward `ocx_index/__testing` at WP-28. |
| 9 | Medium | Plan § Constitution deviations; C-030, WP-13, WP-18; addendum D-048, § E | Two unrecorded deviations from `arch-principles.md` § Code Style. (a) "no `mod.rs`" — the plan mints three: `ocx_cli/src/exit/mod.rs`, `ocx_cli/src/tracing_init/mod.rs`, `trust/mod.rs`; the tree has zero production `mod.rs` today. (b) "Test-only seams … gate `#[cfg(any(test, feature = "__testing"))]`, never a bespoke override" — design § E ships `ocx_util::env::overrides` unconditionally in the release binary as a `pub` table on the credential read path (`env.rs:1823` is `#[cfg(test)]` today), with a tripwire (D-065) standing in for the compiler. The addendum's reason ("a feature would put the seam on the forward list for nothing") is weaker than it reads: a dev-dependency feature (`[dev-dependencies] ocx_util = { features = ["__testing"] }`) crosses the crate boundary under resolver v3 without touching release builds, and the forward-list test already tolerates it. | (a) `exit.rs` + `exit/*.rs`, `tracing_init.rs` + `tracing_init/*.rs`, keep `trust.rs` + `pub mod key_ref;` (the addendum's own alternative). (b) Gate `overrides` on the canonical cfg, enable via dev-dep features, delete D-065's tripwire — or record the deviation in § Constitution deviations with the argument. |
| 10 | Medium | Plan § Parallelization (worktree bootstrap); WP-02 | Every input the builders are told to read is **untracked**: all nine `.claude/artifacts/{plan,design_spec,discover_*,research_*}_crate_split_*.md`, `scripts/edge_inventory.py.draft`, `test/VERBMAP*.json` (6). A worktree cut from `evelynn` HEAD carries none of them, and the addendum's measurement script (`.tmp/hex/plan-crate-split/edge_inventory_full.py`) is gitignored and differs from the `.draft` (byte 35243). WP-02's "from `.draft`" and its "§ 2 exactly" gate have no reachable input in the worktree. The plan also says `external/sigstore-rs` is uninitialised here — it is (`git submodule status`: `2361ffe0`); the bootstrap step still holds for fresh worktrees. | Add a step 0: `chore(claude): commit the crate-split planning artifacts, inventory script and verb-map drafts` on `evelynn` before B1; WP-02 names which script is authoritative. |
| 11 | Low | Plan WP-16 file list | Omits `ocx_cli/src/command/config_test.rs:7` (`preview_managed_config` import — the third CLI consumer beside `config_push.rs` and `api/data/config_test.rs`) and the `exit/` file that re-spells `ManagedConfigPublishError` (`cli/classify.rs:1158` today). Merge predicate rejects the WP on the first. | Add both. |
| 12 | Low | C-049 (v) `cargo doc -D rustdoc::broken_intra_doc_links`; WP-22, WP-25 | Upward intra-doc links survive the boundary harness (comments are stripped, C-006) and red the doc gate only at extraction: `symlink.rs:8,126,160,188,421` → `crate::reference_manager` (`ocx_store`, above `ocx_util`); `trust.rs:723,735,3039` → `managed_config::publish` (`ocx_package_manager`, above `ocx_trust`). Not in either WP's file list. | Name the doc-link rewrites in WP-22/WP-25 (plain text or `ocx_store::…` is impossible from below — use text). |
| 13 | Low | Plan DEC-2 | "`BooleanString` has no `ocx_cli` consumer" is false — `ocx_cli/src/command/self_group/setup.rs:14,386` — but harmless: all three consumers (`env.rs:1845`, `activate.rs:207`, `setup.rs:386`) call `try_from` and only `Display` the error. Confirmed: `value_variants()` renders exactly `"1, y, yes, true, 0, n, no, false"` (`utility/boolean_string.rs:21–31`), `TrueOn`/`FalseOff` absent; no clap value-type use anywhere. No risk. | Fix the sentence. |
| 14 | Low | Plan C-049 (iii), WP-38 | `.claude/tests/test_ai_config.py:776` skips any path with `worktrees` in its parts, so the markdown-reference test scans only `CLAUDE.md` inside `.agents/worktrees/<slug>/` — vacuous there (memory: baseline-in-worktree trap). The glob test (`:362`) is unaffected, so the red-first ritual survives. | Run `task claude:tests` for WP-38 from the main checkout; note it in the Gate line. |

## Q1 — Boundary violations in DEC-11 / § A.4

None. Verified by opening each moved file: `symlink.rs:18,31,65,94` reaches `prelude`, `archive::Error::SymlinkEscape`, `utility::fs::path` and `Error::InternalFile` only — all `ocx_util` after E1 (residue: finding 7's sibling `FileError`). `oci/sign/key_ref.rs:44,117,661` reaches `utility::fs::path::FileReference`, `env::var`, `test::env::lock` — no sigstore type; `KeyBackendKind` (`:371`) is serde-only. `publisher/layer_ref.rs:8,97,101` reaches media-type consts, `oci`, `utility::fs::path`, and `cli::ClassifyExitCode` (dissolves with 1.1). `record/options.rs` is `serde` + `PathBuf`. `DEFAULT_INDEX_BASE_URL` (`oci/index/ocx_index.rs:605`) and `default_ocx_root` (`file_structure.rs:232`, reads `env::var` + `home_directory`) carry no reach. `activation.rs:42–51` reaches env/oci/package/package_manager/project/shell/config/file_structure — all below `ocx_package_manager` once `ocx_shell`/`ocx_trust` rows are added; its only code importer is `cli/classify.rs:105` (→ `ocx_cli`). `managed_config/publish.rs:38–45` reaches config/oci/package/publisher/tls/utility — below `ocx_package_manager`; CLI consumers still resolve (finding 11 names the third). `grep crate::package_manager shell.rs shell/` = 0 hits → no `ocx_shell ↔ ocx_package_manager` cycle. `DialPolicy`'s `ProxyRules` is `oci/ssrf.rs:415` — stays in `ocx_oci`, so `ocx_sign → ocx_oci` only. No ecosystem crate gains a dependency; grimoire's six-crate closure is intact (its *external* closure is larger than C-060 claims — finding 3).

## Q2 — DEC-1

Behaviour-preserving, yes — but for the wrong stated reason, and with one manifest trap (finding 5). Either macro family renders identically under this subscriber; the decision is a diff-size call and should say so.

## Q3 — DEC-2

Confirmed and risk-free (finding 13).

## Q4 — DEC-3

No default directive, `-v` mapping (`log_level.rs`), or doc names `ocx_lib` (0 hits in both files and in `website/src/docs`); no `target = "…"` override in the lib. "Announce once, changelog only" matches CLAUDE.md § Stability tiers. Placement is wrong: WP-21 changes no target (finding 6).

## Q5 — Wave grouping

The plan's regrouping fixed the addendum's `config.rs` collision (env vs config now in different waves) and its `oci/client.rs` split (14a use-lines / 14b body) is sound. Cross-wave pairs checked and clean: `config.rs:175` (w7) vs rest (w8); `oci/client.rs` w7/w8; `file_structure.rs` w6 → w8; `state_store.rs` w8 → w9; `oci/index.rs` w7 → w9; `oci/verify/pipeline.rs` w7 → w9; `lib.rs` w7 (WP-14a) / w8 (WP-17). Same-wave collisions exist in **both** wave 7 and wave 8 (findings 1, 2). B1's three waves are file-disjoint.

## Q6 — E1 timing (D-047)

The direction `ocx_lib → ocx_util` holds for the transparent arms. The reverse trap exists at three sites the addendum did not count (finding 7). No util file constructs `OfflineMode`, `PolicyBlocked` or any other upward variant (grep over `utility/`, `archive/`, `compression*`, `tls.rs`, `symlink.rs`: only local `archive::Error`, `compression::Error`, `singleflight::Error`, `InternalFile`, `SymlinkEscape`, `InvalidBooleanString` (1.7) and `SerializationFailure`).

## Q7 — Scoped-gate honesty

Not honest as written (finding 4). The Gate step's "expected for exit/util/console/oci/config/store/index/package" names the hub rule but not rule (2), which fires on every extraction and every phase-1 WP already pays the full suite through the `ocx_lib → every verb` row. It does conflict with "full verify only at review milestones" in effect if not in wording; the plan should state the count and the mitigation.

## Q8 — One-way-door checks

(a) `ls crates/` = four crate dirs only; a synthetic probe (`members = ["crates/*"]` with a stray `crates/NEXTEST_FLOOR` file) shows Cargo ignores non-directory matches — the glob and the floor file are safe. (b) `dist-workspace.toml` is `members = ["cargo:."]`; only `ocx_cli` opts in via `[package.metadata.dist] dist = true`, all four manifests already carry `publish = false`; 17 lib-only shells are invisible to dist. (c) Seven is wrong — eight (finding 8).

## Q9 — Constitution

Two deviations beyond the recorded one (finding 9). Everything else holds: type names are full words, no `#[non_exhaustive]` on internal enums, lib hosts orchestration (`Publisher::push_package`, `activation` under `package_manager`).

## Q10 — What the plan does not say

1. Its own inputs are untracked (finding 10) — the first `/hex-execute` worktree cannot read the plan, the addendum, the file map or the inventory script.
2. The `log/std` feature (finding 5) — a green `cargo check --workspace` hides a red `cargo nextest run -p ocx_lib` at WP-09.
3. The `task claude:tests` vacuity inside worktrees (finding 14). Submodules, `task schema`, `jobs = 12`, sccache and the 3-worktree cap are all already named.

## Verdict

**Revise before execution** — two Block-tier wave collisions (findings 1, 2) and one unsatisfiable contract (3) are mechanical to fix; nothing here reopens the ADR or the addendum's map.

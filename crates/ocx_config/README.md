# ocx_config

Resolved settings from files and environment: the four config tiers, the managed tier, env-var vocabulary and validation.

**Tier:** ecosystem

**May depend on:** `ocx_trust`, `ocx_oci`, `ocx_util`, `ocx_exit`

**Named dependency exceptions:** none

# ocx_project

Project tier: `ocx.toml` and `ocx.lock`, consent stamps, staged mutation, per-prompt activation sequencing, lazy-loading ladder.

**Tier:** internal

**May depend on:** `ocx_shell`, `ocx_package`, `ocx_index`, `ocx_store`, `ocx_config`, `ocx_trust`, `ocx_oci`, `ocx_util`, `ocx_exit`

**Named dependency exceptions:** none

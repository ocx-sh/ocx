# ocx_script

The Starlark host API for `ocx package test --script`.

**Tier:** internal

**May depend on:** `ocx_store`, `ocx_config`, `ocx_oci`, `ocx_util`, `ocx_console`

**Named dependency exceptions:** none

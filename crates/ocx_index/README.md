# ocx_index

The OCX resolution-index protocol and its local collection.

**Tier:** ecosystem

**May depend on:** `ocx_store`, `ocx_config`, `ocx_oci`, `ocx_util`, `ocx_exit`

**Named dependency exceptions:** none

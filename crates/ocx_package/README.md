# ocx_package

Package identity, metadata, versioning, cascade, authoring and publication.

**Tier:** ecosystem

**May depend on:** `ocx_index`, `ocx_store`, `ocx_config`, `ocx_oci`, `ocx_util`, `ocx_exit`

**Named dependency exceptions:** none

# ocx_test_support

Shared unit-test fixtures (`data`, `fifo`, `pki`) — a dev-dependency of the crates whose tests need them, never a normal dependency.

**Tier:** internal (dev-dependency only)

**May depend on:** none

**Named dependency exceptions:** none

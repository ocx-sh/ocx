# ocx_store

The on-disk layout: three-tier CAS, symlink namespace, package materialisation, shim blobs, local code signing.

**Tier:** internal

**May depend on:** `ocx_config`, `ocx_oci`, `ocx_util`, `ocx_exit`

**Named dependency exceptions:** none

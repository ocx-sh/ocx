extends: base.yml
name: corretto-11
source:
  type: url_index
  generator:
    command: ["uv", "run", "generate.py", "--major", "11"]
    timeout_seconds: 120
versions:
  min: "11.0.0"
  new_per_run: 10

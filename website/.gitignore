/node_modules
/src/node_modules
auto-imports.d.ts

npm-debug.log*
yarn-debug.log*
yarn-error.log*
.vitepress/cache
.vitepress/dist
src/.vitepress/dist
.task/
src/public/casts
src/public/data
# Icons8-licensed assets — must never enter git (license forbids repo
# redistribution). Canonical store: R2 bucket ocx-licensed; local copy is
# the editing workspace, pushed via `task website:deploy:licensed`.
src/public/licensed/
# Schema artifacts are regenerated on every build (see website/schema.taskfile.yml).
src/public/schemas/
src/docs/reference/dependencies.md
src/catalog/

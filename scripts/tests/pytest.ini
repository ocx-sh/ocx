# The gate-tooling suite. Its own ini, so `test/pyproject.toml`'s acceptance
# configuration (registry fixtures, markers, basetemp) never loads here: pytest
# takes the first ini found walking up from the arguments.
[pytest]
addopts = --strict-markers -p no:cacheprovider
